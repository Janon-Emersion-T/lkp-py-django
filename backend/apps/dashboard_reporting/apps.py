from django.apps import AppConfig


class DashboardReportingConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.dashboard_reporting"
    verbose_name = "Dashboard Reporting"
