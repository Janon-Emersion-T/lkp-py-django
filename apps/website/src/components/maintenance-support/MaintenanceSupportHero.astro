<section
  class="maintenance-hero"
  aria-labelledby="maintenance-hero-title"
>
  <div class="site-container maintenance-hero__inner">
    <div class="maintenance-hero__content">
      <p class="maintenance-hero__eyebrow">
        Maintenance & Support
      </p>

      <h1 id="maintenance-hero-title">
        Technology needs attention
        <span>after launch.</span>
      </h1>

      <p class="maintenance-hero__lead">
        We help businesses maintain, troubleshoot and improve
        websites, software and digital systems so they remain
        dependable as technology, users and operating requirements
        change.
      </p>
    </div>

    <aside class="maintenance-hero__aside">
      <span>Operational focus</span>

      <p>
        Prevent issues.<br />
        Respond faster.<br />
        Keep improving.
      </p>
    </aside>
  </div>

  <div
    class="site-container maintenance-hero__signals"
    aria-hidden="true"
  >
    <div>
      <span>01</span>
      <strong>Maintain</strong>
    </div>

    <i></i>

    <div>
      <span>02</span>
      <strong>Monitor</strong>
    </div>

    <i></i>

    <div>
      <span>03</span>
      <strong>Resolve</strong>
    </div>

    <i></i>

    <div>
      <span>04</span>
      <strong>Improve</strong>
    </div>
  </div>
</section>

<style>
  .maintenance-hero {
    position: relative;
    overflow: hidden;
    border-bottom: 1px solid #dde4ed;
    background: #f7f9fc;
    padding-block:
      clamp(4.5rem, 9vw, 8rem)
      clamp(2.8rem, 5vw, 4rem);
  }

  .maintenance-hero::before {
    position: absolute;
    inset: 0;
    background:
      linear-gradient(
        90deg,
        rgba(11, 47, 143, 0.035) 1px,
        transparent 1px
      ),
      linear-gradient(
        rgba(11, 47, 143, 0.035) 1px,
        transparent 1px
      );
    background-size: 4.5rem 4.5rem;
    content: "";
    pointer-events: none;
  }

  .maintenance-hero__inner,
  .maintenance-hero__signals {
    position: relative;
    z-index: 1;
  }

  .maintenance-hero__inner {
    display: grid;
    grid-template-columns:
      minmax(0, 1fr)
      minmax(14rem, 0.3fr);
    align-items: end;
    gap: clamp(3rem, 9vw, 8rem);
  }

  .maintenance-hero__eyebrow {
    margin: 0 0 1rem;
    color: #0b2f8f;
    font-size: 0.67rem;
    font-weight: 800;
    letter-spacing: 0.15em;
    text-transform: uppercase;
  }

  .maintenance-hero h1 {
    max-width: 11ch;
    margin: 0;
    color: #0b1e3a;
    font-family: var(--font-serif);
    font-size: clamp(3.7rem, 8vw, 7.3rem);
    font-weight: 400;
    letter-spacing: -0.06em;
    line-height: 0.92;
  }

  .maintenance-hero h1 span {
    display: block;
    color: #0b2f8f;
  }

  .maintenance-hero__lead {
    max-width: 45rem;
    margin: 1.6rem 0 0;
    color: #687589;
    font-size: clamp(0.9rem, 1.2vw, 1rem);
    line-height: 1.8;
  }

  .maintenance-hero__aside {
    border-left: 1px solid #cad4e1;
    padding-left: 1.25rem;
  }

  .maintenance-hero__aside span {
    color: #8792a2;
    font-size: 0.59rem;
    font-weight: 800;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  .maintenance-hero__aside p {
    margin: 0.55rem 0 0;
    color: #293b53;
    font-size: 0.76rem;
    font-weight: 700;
    line-height: 1.75;
  }

  .maintenance-hero__signals {
    display: grid;
    grid-template-columns:
      auto 1fr
      auto 1fr
      auto 1fr
      auto;
    align-items: center;
    gap: 0.8rem;
    border-top: 1px solid #d9e0e9;
    margin-top: clamp(3.5rem, 7vw, 6rem);
    padding-top: 1.25rem;
  }

  .maintenance-hero__signals div {
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .maintenance-hero__signals span {
    color: #8b96a7;
    font-size: 0.54rem;
    font-weight: 800;
  }

  .maintenance-hero__signals strong {
    color: #53647a;
    font-size: 0.6rem;
  }

  .maintenance-hero__signals i {
    height: 1px;
    background: #ced7e2;
  }

  @media (max-width: 52rem) {
    .maintenance-hero__inner {
      grid-template-columns: 1fr;
      align-items: start;
    }
  }

  @media (max-width: 38rem) {
    .maintenance-hero h1 {
      font-size: clamp(3.1rem, 16vw, 5rem);
    }

    .maintenance-hero__signals {
      display: none;
    }
  }
</style>
