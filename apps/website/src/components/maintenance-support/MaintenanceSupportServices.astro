---
import {
  maintenanceServices,
} from "../../config/maintenance-support";
---

<section
  class="maintenance-services"
  aria-labelledby="maintenance-services-title"
>
  <div class="site-container">
    <header class="maintenance-services__heading">
      <div>
        <p>Maintenance services</p>

        <h2 id="maintenance-services-title">
          Support for the systems your business relies on.
        </h2>
      </div>

      <p>
        Choose the area that matches what needs maintaining,
        improving or supporting.
      </p>
    </header>

    <div class="maintenance-services__list">
      {
        maintenanceServices.map((service) => (
          <a href={service.href}>
            <div class="maintenance-services__number">
              <span>{service.number}</span>
            </div>

            <div class="maintenance-services__content">
              <h3>{service.title}</h3>
              <p>{service.description}</p>
            </div>

            <div class="maintenance-services__includes">
              {
                service.includes.map((item) => (
                  <span>{item}</span>
                ))
              }
            </div>

            <svg
              aria-hidden="true"
              viewBox="0 0 18 18"
              width="18"
              height="18"
            >
              <path
                d="M3 9h12M11 5l4 4-4 4"
                fill="none"
                stroke="currentColor"
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="1.5"
              />
            </svg>
          </a>
        ))
      }
    </div>
  </div>
</section>

<style>
  .maintenance-services {
    padding-block: clamp(4.5rem, 9vw, 8rem);
    background: #f6f8fb;
  }

  .maintenance-services__heading {
    display: grid;
    grid-template-columns:
      minmax(0, 1fr)
      minmax(15rem, 0.42fr);
    align-items: end;
    gap: 3rem;
    margin-bottom: 3rem;
  }

  .maintenance-services__heading
    > div > p {
    margin: 0 0 1rem;
    color: #0b2f8f;
    font-size: 0.64rem;
    font-weight: 800;
    letter-spacing: 0.14em;
    text-transform: uppercase;
  }

  .maintenance-services h2 {
    max-width: 14ch;
    margin: 0;
    color: #0d203c;
    font-family: var(--font-serif);
    font-size: clamp(2.5rem, 5vw, 4.7rem);
    font-weight: 400;
    letter-spacing: -0.05em;
    line-height: 1;
  }

  .maintenance-services__heading > p {
    margin: 0;
    color: #788497;
    font-size: 0.75rem;
    line-height: 1.7;
  }

  .maintenance-services__list {
    border-top: 1px solid #dce3ec;
  }

  .maintenance-services__list > a {
    display: grid;
    grid-template-columns:
      3.2rem
      minmax(13rem, 0.6fr)
      minmax(15rem, 0.7fr)
      auto;
    gap: clamp(1rem, 3vw, 2rem);
    align-items: start;
    border-bottom: 1px solid #dce3ec;
    padding-block: 1.5rem;
    transition:
      background-color 180ms ease,
      padding-inline 180ms ease;
  }

  .maintenance-services__list > a:hover {
    padding-inline: 0.8rem;
    background: #ffffff;
  }

  .maintenance-services__number span {
    color: #0b2f8f;
    font-size: 0.6rem;
    font-weight: 800;
  }

  .maintenance-services h3 {
    margin: 0;
    color: #263850;
    font-size: 0.9rem;
    font-weight: 760;
  }

  .maintenance-services__content p {
    max-width: 35rem;
    margin: 0.45rem 0 0;
    color: #748095;
    font-size: 0.7rem;
    line-height: 1.65;
  }

  .maintenance-services__includes {
    display: flex;
    flex-wrap: wrap;
    gap: 0.4rem;
  }

  .maintenance-services__includes span {
    border: 1px solid #d3dce7;
    border-radius: 999px;
    background: #ffffff;
    color: #657389;
    padding: 0.34rem 0.52rem;
    font-size: 0.54rem;
  }

  .maintenance-services__list svg {
    margin-top: 0.1rem;
    color: #0b2f8f;
    transition: transform 180ms ease;
  }

  .maintenance-services__list > a:hover svg {
    transform: translateX(0.3rem);
  }

  @media (max-width: 56rem) {
    .maintenance-services__list > a {
      grid-template-columns:
        2.5rem
        1fr
        auto;
    }

    .maintenance-services__includes {
      grid-column: 2;
    }
  }

  @media (max-width: 48rem) {
    .maintenance-services__heading {
      grid-template-columns: 1fr;
      align-items: start;
    }
  }

  @media (max-width: 34rem) {
    .maintenance-services__list > a {
      grid-template-columns:
        2rem
        1fr;
    }

    .maintenance-services__list svg {
      display: none;
    }

    .maintenance-services__includes {
      grid-column: 2;
    }
  }
</style>
