<section
  class="services-hero"
  aria-labelledby="services-hero-title"
>
  <div class="site-container services-hero__layout">
    <div>
      <p class="services-hero__eyebrow">
        Our Services
      </p>

      <h1 id="services-hero-title">
        Technology should move
        <span>the business forward.</span>
      </h1>
    </div>

    <div class="services-hero__context">
      <p>
        LKProfessionals works across software, websites, mobile,
        AI, infrastructure, cybersecurity, automation, marketing
        and technology consulting.
      </p>

      <p>
        Different capabilities. One objective: technology that
        performs a useful role in the business.
      </p>

      <div class="services-hero__index">
        <span>13</span>

        <div>
          <strong>service families</strong>
          <small>
            Build · Grow · Operate · Advise
          </small>
        </div>
      </div>
    </div>
  </div>

  <div
    class="site-container services-hero__rail"
    aria-hidden="true"
  >
    <span>Strategy</span>
    <i></i>
    <span>Design</span>
    <i></i>
    <span>Engineering</span>
    <i></i>
    <span>Growth</span>
    <i></i>
    <span>Operations</span>
  </div>
</section>

<style>
  .services-hero {
    position: relative;
    overflow: hidden;
    border-bottom: 1px solid #dde4ed;
    background: #f7f9fc;
    padding-block:
      clamp(5rem, 10vw, 9rem)
      clamp(3rem, 6vw, 4.5rem);
  }

  .services-hero::before {
    position: absolute;
    inset: 0;
    background:
      linear-gradient(
        90deg,
        rgba(11, 47, 143, 0.035) 1px,
        transparent 1px
      ),
      linear-gradient(
        rgba(11, 47, 143, 0.035) 1px,
        transparent 1px
      );
    background-size: 4.5rem 4.5rem;
    content: "";
    pointer-events: none;
  }

  .services-hero__layout,
  .services-hero__rail {
    position: relative;
    z-index: 1;
  }

  .services-hero__layout {
    display: grid;
    grid-template-columns:
      minmax(0, 1fr)
      minmax(17rem, 0.42fr);
    align-items: end;
    gap: clamp(3rem, 9vw, 8rem);
  }

  .services-hero__eyebrow {
    margin: 0 0 1rem;
    color: #0b2f8f;
    font-size: 0.67rem;
    font-weight: 800;
    letter-spacing: 0.15em;
    text-transform: uppercase;
  }

  .services-hero h1 {
    max-width: 11ch;
    margin: 0;
    color: #0b1e3a;
    font-family: var(--font-serif);
    font-size: clamp(4rem, 8.5vw, 7.7rem);
    font-weight: 400;
    letter-spacing: -0.065em;
    line-height: 0.9;
  }

  .services-hero h1 span {
    display: block;
    color: #0b2f8f;
  }

  .services-hero__context {
    border-left: 1px solid #cdd6e2;
    padding-left: 1.4rem;
  }

  .services-hero__context > p {
    margin: 0;
    color: #6d798b;
    font-size: 0.78rem;
    line-height: 1.75;
  }

  .services-hero__context > p + p {
    margin-top: 1rem;
  }

  .services-hero__index {
    display: flex;
    align-items: center;
    gap: 0.9rem;
    border-top: 1px solid #d8e0e9;
    margin-top: 1.5rem;
    padding-top: 1rem;
  }

  .services-hero__index > span {
    color: #0b2f8f;
    font-family: var(--font-serif);
    font-size: 2.8rem;
    line-height: 1;
  }

  .services-hero__index div {
    display: grid;
    gap: 0.2rem;
  }

  .services-hero__index strong {
    color: #293b53;
    font-size: 0.66rem;
    text-transform: uppercase;
    letter-spacing: 0.08em;
  }

  .services-hero__index small {
    color: #8792a2;
    font-size: 0.58rem;
  }

  .services-hero__rail {
    display: grid;
    grid-template-columns:
      auto 1fr auto 1fr auto 1fr auto 1fr auto;
    align-items: center;
    gap: 0.75rem;
    border-top: 1px solid #d9e0e9;
    margin-top: clamp(4rem, 8vw, 7rem);
    padding-top: 1.25rem;
  }

  .services-hero__rail span {
    color: #53647a;
    font-size: 0.58rem;
    font-weight: 750;
  }

  .services-hero__rail i {
    height: 1px;
    background: #cfd8e4;
  }

  @media (max-width: 52rem) {
    .services-hero__layout {
      grid-template-columns: 1fr;
      align-items: start;
    }

    .services-hero__context {
      max-width: 37rem;
    }
  }

  @media (max-width: 38rem) {
    .services-hero h1 {
      font-size: clamp(3.2rem, 16vw, 5.2rem);
    }

    .services-hero__rail {
      display: none;
    }
  }
</style>
