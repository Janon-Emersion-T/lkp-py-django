<section class="about-principles">
  <div class="site-container about-principles__layout">
    <div>
      <p>How we think</p>

      <h2>
        The principles behind the work.
      </h2>
    </div>

    <div class="about-principles__list">
      <div>
        <span>01</span>
        <strong>Understand before implementing</strong>
      </div>

      <div>
        <span>02</span>
        <strong>Build for maintainability</strong>
      </div>

      <div>
        <span>03</span>
        <strong>Communicate clearly</strong>
      </div>

      <div>
        <span>04</span>
        <strong>Remain accountable</strong>
      </div>

      <div>
        <span>05</span>
        <strong>Keep learning</strong>
      </div>

      <div>
        <span>06</span>
        <strong>Think beyond launch</strong>
      </div>
    </div>
  </div>
</section>

<style>
  .about-principles {
    padding-block: clamp(4.5rem, 9vw, 8rem);
    background: #ffffff;
  }

  .about-principles__layout {
    display: grid;
    grid-template-columns:
      minmax(16rem, 0.7fr)
      minmax(0, 1fr);
    gap: clamp(3rem, 9vw, 8rem);
  }

  .about-principles__layout > div:first-child > p {
    margin: 0 0 1rem;
    color: #0b2f8f;
    font-size: 0.64rem;
    font-weight: 800;
    letter-spacing: 0.14em;
    text-transform: uppercase;
  }

  .about-principles h2 {
    max-width: 10ch;
    margin: 0;
    color: #0d203c;
    font-family: var(--font-serif);
    font-size: clamp(2.5rem, 5vw, 4.6rem);
    font-weight: 400;
    letter-spacing: -0.05em;
    line-height: 1;
  }

  .about-principles__list {
    border-top: 1px solid #dce3ec;
  }

  .about-principles__list div {
    display: grid;
    grid-template-columns:
      3rem
      1fr;
    gap: 1rem;
    border-bottom: 1px solid #dce3ec;
    padding-block: 1rem;
  }

  .about-principles__list span {
    color: #8792a2;
    font-size: 0.58rem;
    font-weight: 800;
  }

  .about-principles__list strong {
    color: #293b53;
    font-size: 0.76rem;
  }

  @media (max-width: 52rem) {
    .about-principles__layout {
      grid-template-columns: 1fr;
    }
  }
</style>
