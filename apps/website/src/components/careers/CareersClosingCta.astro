<section class="careers-closing">
  <div class="site-container careers-closing__inner">
    <div>
      <p>Careers at LKProfessionals</p>

      <h2>
        Think you could contribute here?
      </h2>
    </div>

    <div class="careers-closing__actions">
      <a href="#open-positions">
        Explore open positions
      </a>

      <a href="#open-application">
        Send an open application
      </a>
    </div>
  </div>
</section>

<style>
  .careers-closing {
    padding-block: clamp(4rem, 8vw, 6rem);
    background: #0b2f8f;
    color: #f6f3eb;
  }

  .careers-closing__inner {
    display: flex;
    align-items: end;
    justify-content: space-between;
    gap: 3rem;
  }

  .careers-closing p {
    margin: 0 0 0.7rem;
    color: rgba(246,243,235,0.55);
    font-size: 0.62rem;
    font-weight: 800;
    letter-spacing: 0.13em;
    text-transform: uppercase;
  }

  .careers-closing h2 {
    max-width: 12ch;
    margin: 0;
    font-family: var(--font-serif);
    font-size: clamp(2.4rem, 5vw, 4.4rem);
    font-weight: 400;
    letter-spacing: -0.05em;
    line-height: 1;
  }

  .careers-closing__actions {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
  }

  .careers-closing__actions a {
    display: inline-flex;
    min-height: 2.9rem;
    align-items: center;
    border: 1px solid rgba(255,255,255,0.28);
    border-radius: 0.5rem;
    padding-inline: 1rem;
    color: #ffffff;
    font-size: 0.7rem;
    font-weight: 740;
  }

  @media (max-width: 48rem) {
    .careers-closing__inner {
      align-items: flex-start;
      flex-direction: column;
    }
  }
</style>
