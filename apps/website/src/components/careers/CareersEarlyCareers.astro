<section class="careers-early">
  <div class="site-container careers-early__layout">
    <div>
      <p class="careers-section-label">Internships & early careers</p>

      <h2>
        Ability matters. So does potential.
      </h2>
    </div>

    <div>
      <p>
        We welcome interns, trainees, university students completing
        industrial training and candidates building their first serious
        professional experience.
      </p>

      <p>
        A degree can be valuable, but it is not the only evidence of
        capability. Strong portfolios, GitHub work, practical projects,
        certifications and demonstrated technical ability all matter.
      </p>

      <p>
        Internship terms, including compensation, depend on the role,
        candidate capability and arrangement.
      </p>
    </div>
  </div>
</section>

<style>
  .careers-early {
    padding-block: clamp(4.5rem, 9vw, 8rem);
    background: #ffffff;
  }

  .careers-early__layout {
    display: grid;
    grid-template-columns:
      minmax(15rem, 0.75fr)
      minmax(0, 1fr);
    gap: clamp(3rem, 9vw, 8rem);
  }

  .careers-early h2 {
    max-width: 11ch;
    margin: 1rem 0 0;
    color: #0d203c;
    font-family: var(--font-serif);
    font-size: clamp(2.5rem, 5vw, 4.6rem);
    font-weight: 400;
    letter-spacing: -0.05em;
    line-height: 1;
  }

  .careers-early__layout > div:last-child {
    display: grid;
    gap: 1.1rem;
  }

  .careers-early__layout > div:last-child p {
    margin: 0;
    color: #6f7c90;
    font-size: 0.86rem;
    line-height: 1.78;
  }

  @media (max-width: 52rem) {
    .careers-early__layout {
      grid-template-columns: 1fr;
    }
  }
</style>
