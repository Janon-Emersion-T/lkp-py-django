<section class="story-origin" aria-labelledby="story-origin-title">
  <div class="site-container story-origin__layout">
    <div class="story-origin__label">
      <span>01</span>
      <p>Where it began</p>
    </div>

    <div class="story-origin__content">
      <h2 id="story-origin-title">
        Before the company came the work.
      </h2>

      <div class="story-origin__copy">
        <p>
          LKProfessionals traces its beginnings to 2013.
          The foundation was not a large office, a complicated
          organisational chart or a venture-backed launch.
          It was practical IT work and the responsibility that
          comes with solving problems for real people.
        </p>

        <p>
          Those early years created something more valuable than
          presentation: direct experience with changing client
          requirements, evolving technology, operational pressure
          and the difference between something that merely works
          and something that remains dependable.
        </p>
      </div>
    </div>
  </div>
</section>

<style>
  .story-origin {
    padding-block: clamp(5rem, 10vw, 9rem);
    background: #ffffff;
  }

  .story-origin__layout {
    display: grid;
    grid-template-columns:
      minmax(8rem, 0.28fr)
      minmax(0, 1fr);
    gap: clamp(2rem, 6vw, 5rem);
  }

  .story-origin__label {
    display: flex;
    align-items: flex-start;
    gap: 0.8rem;
  }

  .story-origin__label span {
    color: #8995a6;
    font-size: 0.6rem;
    font-weight: 800;
  }

  .story-origin__label p {
    margin: 0;
    color: #0b2f8f;
    font-size: 0.64rem;
    font-weight: 800;
    letter-spacing: 0.13em;
    text-transform: uppercase;
  }

  .story-origin h2 {
    max-width: 11ch;
    margin: 0;
    color: #0d203c;
    font-family: var(--font-serif);
    font-size: clamp(2.8rem, 5.6vw, 5.3rem);
    font-weight: 400;
    letter-spacing: -0.055em;
    line-height: 0.98;
  }

  .story-origin__copy {
    display: grid;
    grid-template-columns:
      repeat(2, minmax(0, 1fr));
    gap: 2rem;
    border-top: 1px solid #dce3ec;
    margin-top: 2.2rem;
    padding-top: 1.5rem;
  }

  .story-origin__copy p {
    margin: 0;
    color: #6f7c90;
    font-size: 0.82rem;
    line-height: 1.8;
  }

  @media (max-width: 48rem) {
    .story-origin__layout {
      grid-template-columns: 1fr;
    }

    .story-origin__copy {
      grid-template-columns: 1fr;
    }
  }
</style>
