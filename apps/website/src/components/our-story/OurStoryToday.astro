<section class="story-today">
  <div class="site-container story-today__layout">
    <div>
      <p>Where we are now</p>

      <h2>
        Still building the company we intend to become.
      </h2>
    </div>

    <div class="story-today__content">
      <p>
        Today, LKProfessionals operates as a Sri Lankan technology
        company with its base in Jaffna and a growing focus on
        international markets.
      </p>

      <p>
        The company is expanding its technical capabilities,
        internal systems, delivery processes and team while
        preserving the practical mindset that shaped its beginning.
      </p>

      <p>
        The story is not presented as finished. The next chapter
        is about turning more than a decade of experience into a
        stronger, more structured and internationally competitive
        technology business.
      </p>

      <div class="story-today__direction">
        <div>
          <span>Foundation</span>
          <strong>Experience since 2013</strong>
        </div>

        <div>
          <span>Identity</span>
          <strong>Sri Lankan technology company</strong>
        </div>

        <div>
          <span>Direction</span>
          <strong>International growth</strong>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .story-today {
    padding-block: clamp(5rem, 10vw, 9rem);
    background: #ffffff;
  }

  .story-today__layout {
    display: grid;
    grid-template-columns:
      minmax(16rem, 0.7fr)
      minmax(0, 1fr);
    gap: clamp(3rem, 9vw, 8rem);
  }

  .story-today__layout > div:first-child > p {
    margin: 0 0 1rem;
    color: #0b2f8f;
    font-size: 0.64rem;
    font-weight: 800;
    letter-spacing: 0.14em;
    text-transform: uppercase;
  }

  .story-today h2 {
    max-width: 11ch;
    margin: 0;
    color: #0d203c;
    font-family: var(--font-serif);
    font-size: clamp(2.5rem, 5vw, 4.6rem);
    font-weight: 400;
    letter-spacing: -0.05em;
    line-height: 1;
  }

  .story-today__content {
    display: grid;
    gap: 1.1rem;
    align-content: start;
  }

  .story-today__content > p {
    max-width: 44rem;
    margin: 0;
    color: #6f7c90;
    font-size: 0.84rem;
    line-height: 1.8;
  }

  .story-today__direction {
    display: grid;
    grid-template-columns:
      repeat(3, minmax(0, 1fr));
    border-top: 1px solid #dce3ec;
    margin-top: 1.3rem;
  }

  .story-today__direction div {
    border-right: 1px solid #dce3ec;
    border-bottom: 1px solid #dce3ec;
    padding: 1rem;
  }

  .story-today__direction div:first-child {
    padding-left: 0;
  }

  .story-today__direction div:last-child {
    border-right: 0;
  }

  .story-today__direction span {
    display: block;
    color: #8792a2;
    font-size: 0.56rem;
    font-weight: 800;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .story-today__direction strong {
    display: block;
    margin-top: 0.45rem;
    color: #293b53;
    font-size: 0.7rem;
  }

  @media (max-width: 52rem) {
    .story-today__layout {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 38rem) {
    .story-today__direction {
      grid-template-columns: 1fr;
    }

    .story-today__direction div,
    .story-today__direction div:first-child {
      border-right: 0;
      padding-inline: 0;
    }
  }
</style>
