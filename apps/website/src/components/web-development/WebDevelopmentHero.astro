<section
  class="web-hero"
  aria-labelledby="web-hero-title"
>
  <div class="site-container web-hero__inner">
    <div class="web-hero__content">
      <p class="web-hero__eyebrow">
        Web Development
      </p>

      <h1 id="web-hero-title">
        Build a website that
        <span>has a job to do.</span>
      </h1>

      <p class="web-hero__lead">
        LKProfessionals designs and develops websites and web
        applications around business objectives — combining
        credibility, performance, discoverability, conversion
        and maintainable engineering in one digital platform.
      </p>
    </div>

    <aside class="web-hero__aside">
      <span>Website objective</span>

      <p>
        Explain clearly.<br />
        Perform reliably.<br />
        Create action.
      </p>
    </aside>
  </div>

  <div
    class="site-container web-hero__flow"
    aria-hidden="true"
  >
    <span>Search</span>
    <i></i>
    <span>Visit</span>
    <i></i>
    <span>Understand</span>
    <i></i>
    <span>Trust</span>
    <i></i>
    <span>Act</span>
  </div>
</section>

<style>
  .web-hero {
    position: relative;
    overflow: hidden;
    border-bottom: 1px solid #dde4ed;
    background: #f7f9fc;
    padding-block:
      clamp(4.5rem, 9vw, 8rem)
      clamp(2.8rem, 5vw, 4rem);
  }

  .web-hero::before {
    position: absolute;
    inset: 0;
    background:
      linear-gradient(
        90deg,
        rgba(11, 47, 143, 0.035) 1px,
        transparent 1px
      ),
      linear-gradient(
        rgba(11, 47, 143, 0.035) 1px,
        transparent 1px
      );
    background-size: 4.5rem 4.5rem;
    content: "";
    pointer-events: none;
  }

  .web-hero__inner,
  .web-hero__flow {
    position: relative;
    z-index: 1;
  }

  .web-hero__inner {
    display: grid;
    grid-template-columns:
      minmax(0, 1fr)
      minmax(14rem, 0.3fr);
    align-items: end;
    gap: clamp(3rem, 9vw, 8rem);
  }

  .web-hero__eyebrow {
    margin: 0 0 1rem;
    color: #0b2f8f;
    font-size: 0.67rem;
    font-weight: 800;
    letter-spacing: 0.15em;
    text-transform: uppercase;
  }

  .web-hero h1 {
    max-width: 11ch;
    margin: 0;
    color: #0b1e3a;
    font-family: var(--font-serif);
    font-size: clamp(3.7rem, 8vw, 7.3rem);
    font-weight: 400;
    letter-spacing: -0.06em;
    line-height: 0.92;
  }

  .web-hero h1 span {
    display: block;
    color: #0b2f8f;
  }

  .web-hero__lead {
    max-width: 47rem;
    margin: 1.6rem 0 0;
    color: #687589;
    font-size: clamp(0.9rem, 1.2vw, 1rem);
    line-height: 1.8;
  }

  .web-hero__aside {
    border-left: 1px solid #cad4e1;
    padding-left: 1.25rem;
  }

  .web-hero__aside span {
    color: #8792a2;
    font-size: 0.59rem;
    font-weight: 800;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  .web-hero__aside p {
    margin: 0.55rem 0 0;
    color: #293b53;
    font-size: 0.76rem;
    font-weight: 700;
    line-height: 1.75;
  }

  .web-hero__flow {
    display: grid;
    grid-template-columns:
      auto 1fr auto 1fr auto 1fr auto 1fr auto;
    align-items: center;
    gap: 0.75rem;
    border-top: 1px solid #d9e0e9;
    margin-top: clamp(3.5rem, 7vw, 6rem);
    padding-top: 1.25rem;
  }

  .web-hero__flow span {
    color: #53647a;
    font-size: 0.58rem;
    font-weight: 750;
  }

  .web-hero__flow i {
    height: 1px;
    background: #cfd8e4;
  }

  @media (max-width: 52rem) {
    .web-hero__inner {
      grid-template-columns: 1fr;
      align-items: start;
    }
  }

  @media (max-width: 38rem) {
    .web-hero h1 {
      font-size: clamp(3.1rem, 16vw, 5rem);
    }

    .web-hero__flow {
      display: none;
    }
  }
</style>
