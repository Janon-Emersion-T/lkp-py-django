---
import {
  webDevelopmentStages,
} from "../../config/web-development";
---

<section
  class="web-process"
  aria-labelledby="web-process-title"
>
  <div class="site-container">
    <header class="web-process__heading">
      <p>From business to browser</p>

      <h2 id="web-process-title">
        Structure before styling. Purpose before pages.
      </h2>
    </header>

    <div class="web-process__steps">
      {
        webDevelopmentStages.map((stage) => (
          <article>
            <span>{stage.number}</span>
            <h3>{stage.title}</h3>
            <p>{stage.description}</p>
          </article>
        ))
      }
    </div>
  </div>
</section>

<style>
  .web-process {
    padding-block: clamp(4.5rem, 9vw, 8rem);
    background: #ffffff;
  }

  .web-process__heading {
    max-width: 48rem;
    margin-bottom: 3rem;
  }

  .web-process__heading > p {
    margin: 0 0 1rem;
    color: #0b2f8f;
    font-size: 0.64rem;
    font-weight: 800;
    letter-spacing: 0.14em;
    text-transform: uppercase;
  }

  .web-process h2 {
    max-width: 13ch;
    margin: 0;
    color: #0d203c;
    font-family: var(--font-serif);
    font-size: clamp(2.5rem, 5vw, 4.6rem);
    font-weight: 400;
    letter-spacing: -0.05em;
    line-height: 1;
  }

  .web-process__steps {
    display: grid;
    grid-template-columns:
      repeat(6, minmax(0, 1fr));
    border-top: 1px solid #dce3ec;
  }

  .web-process article {
    border-right: 1px solid #dce3ec;
    border-bottom: 1px solid #dce3ec;
    padding: 1.3rem 1rem;
  }

  .web-process article:first-child {
    padding-left: 0;
  }

  .web-process article:last-child {
    border-right: 0;
  }

  .web-process article > span {
    color: #0b2f8f;
    font-size: 0.58rem;
    font-weight: 800;
  }

  .web-process h3 {
    margin: 1rem 0 0;
    color: #283a52;
    font-size: 0.8rem;
  }

  .web-process article p {
    margin: 0.55rem 0 0;
    color: #788497;
    font-size: 0.67rem;
    line-height: 1.65;
  }

  @media (max-width: 64rem) {
    .web-process__steps {
      grid-template-columns:
        repeat(3, minmax(0, 1fr));
    }
  }

  @media (max-width: 42rem) {
    .web-process__steps {
      grid-template-columns:
        repeat(2, minmax(0, 1fr));
    }
  }

  @media (max-width: 30rem) {
    .web-process__steps {
      grid-template-columns: 1fr;
    }

    .web-process article,
    .web-process article:first-child {
      border-right: 0;
      padding-inline: 0;
    }
  }
</style>
