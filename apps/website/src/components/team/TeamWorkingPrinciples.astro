<section class="team-principles">
  <div class="site-container team-principles__layout">
    <div>
      <p>How we work</p>

      <h2>
        Different disciplines. One standard.
      </h2>
    </div>

    <div class="team-principles__list">
      <div>
        <span>01</span>
        <strong>Professional accountability</strong>
      </div>

      <div>
        <span>02</span>
        <strong>Clear communication</strong>
      </div>

      <div>
        <span>03</span>
        <strong>Continuous learning</strong>
      </div>

      <div>
        <span>04</span>
        <strong>Quality before shortcuts</strong>
      </div>
    </div>
  </div>
</section>

<style>
  .team-principles {
    padding-block:
      clamp(4rem, 8vw, 7rem);
    background: #f6f8fb;
  }

  .team-principles__layout {
    display: grid;
    grid-template-columns:
      minmax(16rem, .7fr)
      minmax(0, 1fr);
    gap: clamp(3rem, 8vw, 7rem);
  }

  .team-principles__layout
    > div:first-child > p {
    margin: 0 0 .9rem;
    color: #0b2f8f;
    font-size: .64rem;
    font-weight: 800;
    letter-spacing: .14em;
    text-transform: uppercase;
  }

  .team-principles h2 {
    max-width: 11ch;
    margin: 0;
    color: #0d203c;
    font-family: var(--font-serif);
    font-size:
      clamp(2.5rem, 5vw, 4.5rem);
    font-weight: 400;
    letter-spacing: -.05em;
    line-height: 1;
  }

  .team-principles__list {
    border-top:
      1px solid #dce3ec;
  }

  .team-principles__list div {
    display: grid;
    grid-template-columns:
      3rem 1fr;
    gap: 1rem;
    border-bottom:
      1px solid #dce3ec;
    padding-block: 1.15rem;
  }

  .team-principles__list span {
    color: #8792a2;
    font-size: .6rem;
    font-weight: 800;
  }

  .team-principles__list strong {
    color: #283a52;
    font-size: .78rem;
  }

  @media (max-width: 48rem) {
    .team-principles__layout {
      grid-template-columns: 1fr;
    }
  }
</style>
