<section class="mv-hero" aria-labelledby="mv-hero-title">
  <div class="site-container mv-hero__inner">
    <div class="mv-hero__content">
      <p class="mv-hero__eyebrow">
        Mission & Vision
      </p>

      <h1 id="mv-hero-title">
        Technology with
        <span>purpose and direction.</span>
      </h1>

      <p class="mv-hero__lead">
        LKProfessionals exists to help organisations use technology
        more effectively — not simply by delivering digital products,
        but by creating systems, services and strategies that remain
        useful as businesses evolve.
      </p>
    </div>

    <aside class="mv-hero__aside">
      <span>Our direction</span>

      <p>
        Useful technology.<br />
        Responsible delivery.<br />
        Sustainable progress.
      </p>
    </aside>
  </div>
</section>

<style>
  .mv-hero {
    position: relative;
    overflow: hidden;
    border-bottom: 1px solid #dde4ed;
    background: #f7f9fc;
    padding-block:
      clamp(4.5rem, 9vw, 8rem)
      clamp(4rem, 8vw, 7rem);
  }

  .mv-hero::before {
    position: absolute;
    inset: 0;
    background:
      linear-gradient(
        90deg,
        rgba(11, 47, 143, 0.035) 1px,
        transparent 1px
      ),
      linear-gradient(
        rgba(11, 47, 143, 0.035) 1px,
        transparent 1px
      );
    background-size: 4.5rem 4.5rem;
    content: "";
    pointer-events: none;
  }

  .mv-hero__inner {
    position: relative;
    z-index: 1;
    display: grid;
    grid-template-columns:
      minmax(0, 1fr)
      minmax(14rem, 0.3fr);
    align-items: end;
    gap: clamp(3rem, 9vw, 8rem);
  }

  .mv-hero__eyebrow {
    margin: 0 0 1rem;
    color: #0b2f8f;
    font-size: 0.67rem;
    font-weight: 800;
    letter-spacing: 0.15em;
    text-transform: uppercase;
  }

  .mv-hero h1 {
    max-width: 11ch;
    margin: 0;
    color: #0b1e3a;
    font-family: var(--font-serif);
    font-size: clamp(3.7rem, 8vw, 7.3rem);
    font-weight: 400;
    letter-spacing: -0.06em;
    line-height: 0.92;
  }

  .mv-hero h1 span {
    display: block;
    color: #0b2f8f;
  }

  .mv-hero__lead {
    max-width: 44rem;
    margin: 1.6rem 0 0;
    color: #687589;
    font-size: clamp(0.9rem, 1.2vw, 1rem);
    line-height: 1.8;
  }

  .mv-hero__aside {
    border-left: 1px solid #cad4e1;
    padding-left: 1.25rem;
  }

  .mv-hero__aside span {
    color: #8792a2;
    font-size: 0.59rem;
    font-weight: 800;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  .mv-hero__aside p {
    margin: 0.55rem 0 0;
    color: #293b53;
    font-size: 0.76rem;
    font-weight: 700;
    line-height: 1.75;
  }

  @media (max-width: 50rem) {
    .mv-hero__inner {
      grid-template-columns: 1fr;
      align-items: start;
    }
  }

  @media (max-width: 36rem) {
    .mv-hero h1 {
      font-size: clamp(3.1rem, 16vw, 5rem);
    }
  }
</style>
