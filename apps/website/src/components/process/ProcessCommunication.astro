<section class="process-communication">
  <div class="site-container process-communication__layout">
    <div>
      <p>Visibility throughout</p>

      <h2>
        You should know where the work stands.
      </h2>
    </div>

    <div class="process-communication__content">
      <p>
        Good delivery depends on more than technical execution.
        We aim to keep the client informed about progress,
        decisions, dependencies and issues throughout the work.
      </p>

      <div class="process-communication__items">
        <article>
          <span>01</span>
          <strong>Clear points of contact</strong>
          <p>
            Responsibility should be identifiable on both sides
            of the engagement.
          </p>
        </article>

        <article>
          <span>02</span>
          <strong>Progress visibility</strong>
          <p>
            Reviews and updates keep delivery from becoming a
            black box.
          </p>
        </article>

        <article>
          <span>03</span>
          <strong>Change control</strong>
          <p>
            Material changes to scope or assumptions should be
            identified rather than silently absorbed.
          </p>
        </article>

        <article>
          <span>04</span>
          <strong>Documented handover</strong>
          <p>
            Important access, information and operational
            responsibilities should survive the project.
          </p>
        </article>
      </div>
    </div>
  </div>
</section>

<style>
  .process-communication {
    padding-block: clamp(4.5rem, 9vw, 8rem);
    background: #ffffff;
  }

  .process-communication__layout {
    display: grid;
    grid-template-columns:
      minmax(16rem, 0.68fr)
      minmax(0, 1fr);
    gap: clamp(3rem, 9vw, 8rem);
  }

  .process-communication__layout
    > div:first-child > p {
    margin: 0 0 1rem;
    color: #0b2f8f;
    font-size: 0.64rem;
    font-weight: 800;
    letter-spacing: 0.14em;
    text-transform: uppercase;
  }

  .process-communication h2 {
    max-width: 11ch;
    margin: 0;
    color: #0d203c;
    font-family: var(--font-serif);
    font-size: clamp(2.5rem, 5vw, 4.6rem);
    font-weight: 400;
    letter-spacing: -0.05em;
    line-height: 1;
  }

  .process-communication__content > p {
    max-width: 42rem;
    margin: 0 0 2.2rem;
    color: #6f7c90;
    font-size: 0.86rem;
    line-height: 1.8;
  }

  .process-communication__items {
    border-top: 1px solid #dce3ec;
  }

  .process-communication article {
    display: grid;
    grid-template-columns:
      2.8rem
      minmax(11rem, 0.45fr)
      minmax(0, 1fr);
    gap: 1rem;
    border-bottom: 1px solid #dce3ec;
    padding-block: 1rem;
  }

  .process-communication article > span {
    color: #8792a2;
    font-size: 0.58rem;
    font-weight: 800;
  }

  .process-communication article strong {
    color: #293b53;
    font-size: 0.74rem;
  }

  .process-communication article p {
    margin: 0;
    color: #7a8798;
    font-size: 0.68rem;
    line-height: 1.6;
  }

  @media (max-width: 52rem) {
    .process-communication__layout {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 36rem) {
    .process-communication article {
      grid-template-columns:
        2rem
        1fr;
    }

    .process-communication article p {
      grid-column: 2;
    }
  }
</style>
