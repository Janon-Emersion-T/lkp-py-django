<section
  class="insights-hero"
  aria-labelledby="insights-hero-title"
>
  <div class="site-container insights-hero__layout">
    <div class="insights-hero__heading">
      <p class="insights-hero__eyebrow">
        Insights
      </p>

      <h1 id="insights-hero-title">
        Useful thinking about
        <span>technology and business.</span>
      </h1>
    </div>

    <div class="insights-hero__context">
      <p>
        Practical articles, tutorials and commentary from
        LKProfessionals on websites, software, search, digital
        marketing, AI and the technology organisations rely on.
      </p>

      <p>
        Written to explain what matters, how it works and where
        technology genuinely creates value.
      </p>
    </div>
  </div>

  <div
    class="site-container insights-hero__rail"
    aria-hidden="true"
  >
    <span>Technology</span>
    <i></i>
    <span>Engineering</span>
    <i></i>
    <span>Search</span>
    <i></i>
    <span>Growth</span>
    <i></i>
    <span>Operations</span>
  </div>
</section>

<style>
  .insights-hero {
    position: relative;
    overflow: hidden;
    border-bottom: 1px solid #dde4ed;
    background: #f7f9fc;
    padding-block:
      clamp(5rem, 10vw, 9rem)
      clamp(3rem, 6vw, 4.5rem);
  }

  .insights-hero::before {
    position: absolute;
    inset: 0;
    background:
      linear-gradient(
        90deg,
        rgba(11, 47, 143, 0.035) 1px,
        transparent 1px
      ),
      linear-gradient(
        rgba(11, 47, 143, 0.035) 1px,
        transparent 1px
      );
    background-size: 4.5rem 4.5rem;
    content: "";
    pointer-events: none;
  }

  .insights-hero__layout,
  .insights-hero__rail {
    position: relative;
    z-index: 1;
  }

  .insights-hero__layout {
    display: grid;
    grid-template-columns:
      minmax(0, 1fr)
      minmax(17rem, 0.42fr);
    align-items: end;
    gap: clamp(3rem, 9vw, 8rem);
  }

  .insights-hero__eyebrow {
    margin: 0 0 1rem;
    color: #0b2f8f;
    font-size: 0.67rem;
    font-weight: 800;
    letter-spacing: 0.15em;
    text-transform: uppercase;
  }

  .insights-hero h1 {
    max-width: 11ch;
    margin: 0;
    color: #0b1e3a;
    font-family: var(--font-serif);
    font-size: clamp(4rem, 8.5vw, 7.7rem);
    font-weight: 400;
    letter-spacing: -0.065em;
    line-height: 0.9;
  }

  .insights-hero h1 span {
    display: block;
    color: #0b2f8f;
  }

  .insights-hero__context {
    border-left: 1px solid #cdd6e2;
    padding-left: 1.4rem;
  }

  .insights-hero__context p {
    margin: 0;
    color: #6d798b;
    font-size: 0.78rem;
    line-height: 1.75;
  }

  .insights-hero__context p + p {
    margin-top: 1rem;
  }

  .insights-hero__rail {
    display: grid;
    grid-template-columns:
      auto 1fr auto 1fr auto 1fr auto 1fr auto;
    align-items: center;
    gap: 0.75rem;
    border-top: 1px solid #d9e0e9;
    margin-top: clamp(4rem, 8vw, 7rem);
    padding-top: 1.25rem;
  }

  .insights-hero__rail span {
    color: #53647a;
    font-size: 0.58rem;
    font-weight: 750;
  }

  .insights-hero__rail i {
    height: 1px;
    background: #cfd8e4;
  }

  @media (max-width: 52rem) {
    .insights-hero__layout {
      grid-template-columns: 1fr;
      align-items: start;
    }

    .insights-hero__context {
      max-width: 37rem;
    }
  }

  @media (max-width: 38rem) {
    .insights-hero h1 {
      font-size: clamp(3.2rem, 16vw, 5.2rem);
    }

    .insights-hero__rail {
      display: none;
    }
  }
</style>
