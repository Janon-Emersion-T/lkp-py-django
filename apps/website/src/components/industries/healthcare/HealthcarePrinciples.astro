---
import {
  healthcarePrinciples,
} from "../../../config/healthcare-industry";
---

<section class="healthcare-principles">
  <div class="site-container healthcare-principles__layout">
    <div>
      <p>Healthcare technology principles</p>

      <h2>
        Sensitive systems need restraint as much as capability.
      </h2>
    </div>

    <div class="healthcare-principles__list">
      {
        healthcarePrinciples.map((principle) => (
          <article>
            <span>{principle.number}</span>

            <div>
              <h3>{principle.title}</h3>
              <p>{principle.description}</p>
            </div>
          </article>
        ))
      }
    </div>
  </div>
</section>

<style>
  .healthcare-principles {
    padding-block: clamp(5rem, 10vw, 9rem);
    background: #0b2f8f;
    color: #f6f3eb;
  }

  .healthcare-principles__layout {
    display: grid;
    grid-template-columns:
      minmax(17rem, 0.72fr)
      minmax(0, 1fr);
    gap: clamp(3rem, 8vw, 7rem);
  }

  .healthcare-principles__layout
    > div:first-child > p {
    margin: 0 0 1rem;
    color: rgba(246, 243, 235, 0.56);
    font-size: 0.64rem;
    font-weight: 800;
    letter-spacing: 0.14em;
    text-transform: uppercase;
  }

  .healthcare-principles h2 {
    max-width: 12ch;
    margin: 0;
    font-family: var(--font-serif);
    font-size: clamp(2.6rem, 5vw, 4.7rem);
    font-weight: 400;
    letter-spacing: -0.05em;
    line-height: 1;
  }

  .healthcare-principles__list {
    border-top: 1px solid rgba(255, 255, 255, 0.18);
  }

  .healthcare-principles article {
    display: grid;
    grid-template-columns: 3rem 1fr;
    gap: 1rem;
    border-bottom: 1px solid rgba(255, 255, 255, 0.18);
    padding-block: 1.2rem;
  }

  .healthcare-principles article > span {
    color: rgba(246, 243, 235, 0.4);
    font-size: 0.58rem;
    font-weight: 800;
  }

  .healthcare-principles h3 {
    margin: 0;
    color: #ffffff;
    font-size: 0.82rem;
  }

  .healthcare-principles article p {
    max-width: 42rem;
    margin: 0.45rem 0 0;
    color: rgba(246, 243, 235, 0.65);
    font-size: 0.7rem;
    line-height: 1.65;
  }

  @media (max-width: 52rem) {
    .healthcare-principles__layout {
      grid-template-columns: 1fr;
    }
  }
</style>
