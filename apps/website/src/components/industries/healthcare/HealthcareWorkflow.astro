---
import {
  healthcareWorkflows,
} from "../../../config/healthcare-industry";
---

<section
  class="healthcare-workflow"
  aria-labelledby="healthcare-workflow-title"
>
  <div class="site-container">
    <header class="healthcare-workflow__heading">
      <p>Connected operations</p>

      <h2 id="healthcare-workflow-title">
        Design around the full service journey.
      </h2>
    </header>

    <div class="healthcare-workflow__steps">
      {
        healthcareWorkflows.map((workflow) => (
          <article>
            <span>{workflow.number}</span>
            <h3>{workflow.title}</h3>
            <p>{workflow.description}</p>
          </article>
        ))
      }
    </div>
  </div>
</section>

<style>
  .healthcare-workflow {
    padding-block: clamp(5rem, 10vw, 9rem);
    background: #ffffff;
  }

  .healthcare-workflow__heading {
    max-width: 49rem;
    margin-bottom: 3.5rem;
  }

  .healthcare-workflow__heading > p {
    margin: 0 0 1rem;
    color: #0b2f8f;
    font-size: 0.64rem;
    font-weight: 800;
    letter-spacing: 0.14em;
    text-transform: uppercase;
  }

  .healthcare-workflow h2 {
    max-width: 12ch;
    margin: 0;
    color: #0d203c;
    font-family: var(--font-serif);
    font-size: clamp(2.7rem, 5vw, 4.8rem);
    font-weight: 400;
    letter-spacing: -0.05em;
    line-height: 1;
  }

  .healthcare-workflow__steps {
    display: grid;
    grid-template-columns:
      repeat(5, minmax(0, 1fr));
    border-top: 1px solid #dce3ec;
  }

  .healthcare-workflow article {
    border-right: 1px solid #dce3ec;
    border-bottom: 1px solid #dce3ec;
    padding: 1.3rem 1rem;
  }

  .healthcare-workflow article:first-child {
    padding-left: 0;
  }

  .healthcare-workflow article:last-child {
    border-right: 0;
  }

  .healthcare-workflow article > span {
    color: #0b2f8f;
    font-size: 0.56rem;
    font-weight: 800;
  }

  .healthcare-workflow h3 {
    margin: 1rem 0 0;
    color: #293b53;
    font-size: 0.79rem;
  }

  .healthcare-workflow article p {
    margin: 0.55rem 0 0;
    color: #788497;
    font-size: 0.66rem;
    line-height: 1.65;
  }

  @media (max-width: 62rem) {
    .healthcare-workflow__steps {
      grid-template-columns:
        repeat(3, minmax(0, 1fr));
    }
  }

  @media (max-width: 42rem) {
    .healthcare-workflow__steps {
      grid-template-columns:
        repeat(2, minmax(0, 1fr));
    }
  }

  @media (max-width: 30rem) {
    .healthcare-workflow__steps {
      grid-template-columns: 1fr;
    }

    .healthcare-workflow article,
    .healthcare-workflow article:first-child {
      border-right: 0;
      padding-inline: 0;
    }
  }
</style>
