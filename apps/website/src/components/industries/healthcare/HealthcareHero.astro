<section
  class="healthcare-hero"
  aria-labelledby="healthcare-hero-title"
>
  <div class="site-container healthcare-hero__layout">
    <div>
      <p class="healthcare-hero__eyebrow">
        Industries / Healthcare
      </p>

      <h1 id="healthcare-hero-title">
        Technology for healthcare
        <span>where reliability and trust matter.</span>
      </h1>
    </div>

    <div class="healthcare-hero__context">
      <p>
        Healthcare organisations depend on technology for
        appointments, administration, communications, operational
        records and increasingly connected service delivery.
      </p>

      <p>
        LKProfessionals develops and supports systems around those
        workflows with an emphasis on controlled access, reliability,
        maintainability and practical operational value.
      </p>
    </div>
  </div>

  <div
    class="site-container healthcare-hero__rail"
    aria-hidden="true"
  >
    <span>Access</span>
    <i></i>
    <span>Coordinate</span>
    <i></i>
    <span>Operate</span>
    <i></i>
    <span>Protect</span>
    <i></i>
    <span>Review</span>
  </div>
</section>

<style>
  .healthcare-hero {
    position: relative;
    overflow: hidden;
    border-bottom: 1px solid #dde4ed;
    background: #f7f9fc;
    padding-block:
      clamp(5rem, 10vw, 9rem)
      clamp(3rem, 6vw, 4.5rem);
  }

  .healthcare-hero::before {
    position: absolute;
    inset: 0;
    background:
      linear-gradient(
        90deg,
        rgba(11, 47, 143, 0.035) 1px,
        transparent 1px
      ),
      linear-gradient(
        rgba(11, 47, 143, 0.035) 1px,
        transparent 1px
      );
    background-size: 4.5rem 4.5rem;
    content: "";
    pointer-events: none;
  }

  .healthcare-hero__layout,
  .healthcare-hero__rail {
    position: relative;
    z-index: 1;
  }

  .healthcare-hero__layout {
    display: grid;
    grid-template-columns:
      minmax(0, 1fr)
      minmax(17rem, 0.4fr);
    align-items: end;
    gap: clamp(3rem, 9vw, 8rem);
  }

  .healthcare-hero__eyebrow {
    margin: 0 0 1rem;
    color: #0b2f8f;
    font-size: 0.65rem;
    font-weight: 800;
    letter-spacing: 0.15em;
    text-transform: uppercase;
  }

  .healthcare-hero h1 {
    max-width: 12ch;
    margin: 0;
    color: #0b1e3a;
    font-family: var(--font-serif);
    font-size: clamp(3.8rem, 8vw, 7.3rem);
    font-weight: 400;
    letter-spacing: -0.06em;
    line-height: 0.92;
  }

  .healthcare-hero h1 span {
    display: block;
    color: #0b2f8f;
  }

  .healthcare-hero__context {
    border-left: 1px solid #cdd6e2;
    padding-left: 1.4rem;
  }

  .healthcare-hero__context p {
    margin: 0;
    color: #6d798b;
    font-size: 0.78rem;
    line-height: 1.75;
  }

  .healthcare-hero__context p + p {
    margin-top: 1rem;
  }

  .healthcare-hero__rail {
    display: grid;
    grid-template-columns:
      auto 1fr auto 1fr auto 1fr auto 1fr auto;
    align-items: center;
    gap: 0.75rem;
    border-top: 1px solid #d9e0e9;
    margin-top: clamp(4rem, 8vw, 7rem);
    padding-top: 1.25rem;
  }

  .healthcare-hero__rail span {
    color: #53647a;
    font-size: 0.58rem;
    font-weight: 750;
  }

  .healthcare-hero__rail i {
    height: 1px;
    background: #cfd8e4;
  }

  @media (max-width: 52rem) {
    .healthcare-hero__layout {
      grid-template-columns: 1fr;
      align-items: start;
    }
  }

  @media (max-width: 38rem) {
    .healthcare-hero h1 {
      font-size: clamp(3rem, 15vw, 5rem);
    }

    .healthcare-hero__rail {
      display: none;
    }
  }
</style>
