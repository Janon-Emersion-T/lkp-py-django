<section
  class="sb-hero"
  aria-labelledby="sb-hero-title"
>
  <div class="site-container sb-hero__layout">
    <div>
      <p class="sb-hero__eyebrow">
        Industries / Small Business
      </p>

      <h1 id="sb-hero-title">
        Technology that fits
        <span>the business you actually run.</span>
      </h1>
    </div>

    <div class="sb-hero__context">
      <p>
        Small and growing businesses need technology that improves
        the operation without creating unnecessary cost, complexity
        or dependence.
      </p>

      <p>
        LKProfessionals helps businesses establish the right digital
        foundation, automate recurring work and introduce more
        capable systems as the organisation grows.
      </p>
    </div>
  </div>

  <div
    class="site-container sb-hero__rail"
    aria-hidden="true"
  >
    <span>Establish</span>
    <i></i>
    <span>Sell</span>
    <i></i>
    <span>Operate</span>
    <i></i>
    <span>Automate</span>
    <i></i>
    <span>Grow</span>
  </div>
</section>

<style>
  .sb-hero {
    position: relative;
    overflow: hidden;
    border-bottom: 1px solid #dde4ed;
    background: #f7f9fc;
    padding-block:
      clamp(5rem, 10vw, 9rem)
      clamp(3rem, 6vw, 4.5rem);
  }

  .sb-hero::before {
    position: absolute;
    inset: 0;
    background:
      linear-gradient(
        90deg,
        rgba(11, 47, 143, 0.035) 1px,
        transparent 1px
      ),
      linear-gradient(
        rgba(11, 47, 143, 0.035) 1px,
        transparent 1px
      );
    background-size: 4.5rem 4.5rem;
    content: "";
    pointer-events: none;
  }

  .sb-hero__layout,
  .sb-hero__rail {
    position: relative;
    z-index: 1;
  }

  .sb-hero__layout {
    display: grid;
    grid-template-columns:
      minmax(0, 1fr)
      minmax(17rem, 0.4fr);
    align-items: end;
    gap: clamp(3rem, 9vw, 8rem);
  }

  .sb-hero__eyebrow {
    margin: 0 0 1rem;
    color: #0b2f8f;
    font-size: 0.65rem;
    font-weight: 800;
    letter-spacing: 0.15em;
    text-transform: uppercase;
  }

  .sb-hero h1 {
    max-width: 12ch;
    margin: 0;
    color: #0b1e3a;
    font-family: var(--font-serif);
    font-size: clamp(3.8rem, 8vw, 7.3rem);
    font-weight: 400;
    letter-spacing: -0.06em;
    line-height: 0.92;
  }

  .sb-hero h1 span {
    display: block;
    color: #0b2f8f;
  }

  .sb-hero__context {
    border-left: 1px solid #cdd6e2;
    padding-left: 1.4rem;
  }

  .sb-hero__context p {
    margin: 0;
    color: #6d798b;
    font-size: 0.78rem;
    line-height: 1.75;
  }

  .sb-hero__context p + p {
    margin-top: 1rem;
  }

  .sb-hero__rail {
    display: grid;
    grid-template-columns:
      auto 1fr auto 1fr auto 1fr auto 1fr auto;
    align-items: center;
    gap: 0.75rem;
    border-top: 1px solid #d9e0e9;
    margin-top: clamp(4rem, 8vw, 7rem);
    padding-top: 1.25rem;
  }

  .sb-hero__rail span {
    color: #53647a;
    font-size: 0.58rem;
    font-weight: 750;
  }

  .sb-hero__rail i {
    height: 1px;
    background: #cfd8e4;
  }

  @media (max-width: 52rem) {
    .sb-hero__layout {
      grid-template-columns: 1fr;
      align-items: start;
    }
  }

  @media (max-width: 38rem) {
    .sb-hero h1 {
      font-size: clamp(3rem, 15vw, 5rem);
    }

    .sb-hero__rail {
      display: none;
    }
  }
</style>
