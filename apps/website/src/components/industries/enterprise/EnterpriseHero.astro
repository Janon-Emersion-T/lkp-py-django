<section
  class="enterprise-hero"
  aria-labelledby="enterprise-hero-title"
>
  <div class="site-container enterprise-hero__layout">
    <div>
      <p class="enterprise-hero__eyebrow">
        Industries / Enterprise
      </p>

      <h1 id="enterprise-hero-title">
        Modernise complex systems
        <span>without losing operational control.</span>
      </h1>
    </div>

    <div class="enterprise-hero__context">
      <p>
        Enterprise technology operates inside established processes,
        multiple teams, legacy systems and higher expectations for
        reliability, security and governance.
      </p>

      <p>
        LKProfessionals helps organisations modernise, integrate and
        automate technology in controlled stages rather than treating
        transformation as a single replacement project.
      </p>
    </div>
  </div>

  <div
    class="site-container enterprise-hero__rail"
    aria-hidden="true"
  >
    <span>Assess</span>
    <i></i>
    <span>Integrate</span>
    <i></i>
    <span>Modernise</span>
    <i></i>
    <span>Govern</span>
    <i></i>
    <span>Operate</span>
  </div>
</section>

<style>
  .enterprise-hero {
    position: relative;
    overflow: hidden;
    border-bottom: 1px solid #dde4ed;
    background: #f7f9fc;
    padding-block:
      clamp(5rem, 10vw, 9rem)
      clamp(3rem, 6vw, 4.5rem);
  }

  .enterprise-hero::before {
    position: absolute;
    inset: 0;
    background:
      linear-gradient(
        90deg,
        rgba(11, 47, 143, 0.035) 1px,
        transparent 1px
      ),
      linear-gradient(
        rgba(11, 47, 143, 0.035) 1px,
        transparent 1px
      );
    background-size: 4.5rem 4.5rem;
    content: "";
    pointer-events: none;
  }

  .enterprise-hero__layout,
  .enterprise-hero__rail {
    position: relative;
    z-index: 1;
  }

  .enterprise-hero__layout {
    display: grid;
    grid-template-columns:
      minmax(0, 1fr)
      minmax(17rem, 0.4fr);
    align-items: end;
    gap: clamp(3rem, 9vw, 8rem);
  }

  .enterprise-hero__eyebrow {
    margin: 0 0 1rem;
    color: #0b2f8f;
    font-size: 0.65rem;
    font-weight: 800;
    letter-spacing: 0.15em;
    text-transform: uppercase;
  }

  .enterprise-hero h1 {
    max-width: 12ch;
    margin: 0;
    color: #0b1e3a;
    font-family: var(--font-serif);
    font-size: clamp(3.8rem, 8vw, 7.3rem);
    font-weight: 400;
    letter-spacing: -0.06em;
    line-height: 0.92;
  }

  .enterprise-hero h1 span {
    display: block;
    color: #0b2f8f;
  }

  .enterprise-hero__context {
    border-left: 1px solid #cdd6e2;
    padding-left: 1.4rem;
  }

  .enterprise-hero__context p {
    margin: 0;
    color: #6d798b;
    font-size: 0.78rem;
    line-height: 1.75;
  }

  .enterprise-hero__context p + p {
    margin-top: 1rem;
  }

  .enterprise-hero__rail {
    display: grid;
    grid-template-columns:
      auto 1fr auto 1fr auto 1fr auto 1fr auto;
    align-items: center;
    gap: 0.75rem;
    border-top: 1px solid #d9e0e9;
    margin-top: clamp(4rem, 8vw, 7rem);
    padding-top: 1.25rem;
  }

  .enterprise-hero__rail span {
    color: #53647a;
    font-size: 0.58rem;
    font-weight: 750;
  }

  .enterprise-hero__rail i {
    height: 1px;
    background: #cfd8e4;
  }

  @media (max-width: 52rem) {
    .enterprise-hero__layout {
      grid-template-columns: 1fr;
      align-items: start;
    }
  }

  @media (max-width: 38rem) {
    .enterprise-hero h1 {
      font-size: clamp(3rem, 15vw, 5rem);
    }

    .enterprise-hero__rail {
      display: none;
    }
  }
</style>
