---
import {
  constructionPrinciples,
} from "../../../config/construction-industry";
---

<section class="construction-principles">
  <div class="site-container construction-principles__layout">
    <div>
      <p>Construction technology principles</p>

      <h2>
        Project software should reduce uncertainty, not create more reporting work.
      </h2>
    </div>

    <div class="construction-principles__list">
      {
        constructionPrinciples.map((principle) => (
          <article>
            <span>{principle.number}</span>

            <div>
              <h3>{principle.title}</h3>
              <p>{principle.description}</p>
            </div>
          </article>
        ))
      }
    </div>
  </div>
</section>

<style>
  .construction-principles {
    padding-block: clamp(5rem, 10vw, 9rem);
    background: #0b2f8f;
    color: #f6f3eb;
  }

  .construction-principles__layout {
    display: grid;
    grid-template-columns:
      minmax(17rem, 0.72fr)
      minmax(0, 1fr);
    gap: clamp(3rem, 8vw, 7rem);
  }

  .construction-principles__layout
    > div:first-child > p {
    margin: 0 0 1rem;
    color: rgba(246, 243, 235, 0.56);
    font-size: 0.64rem;
    font-weight: 800;
    letter-spacing: 0.14em;
    text-transform: uppercase;
  }

  .construction-principles h2 {
    max-width: 12ch;
    margin: 0;
    font-family: var(--font-serif);
    font-size: clamp(2.6rem, 5vw, 4.7rem);
    font-weight: 400;
    letter-spacing: -0.05em;
    line-height: 1;
  }

  .construction-principles__list {
    border-top: 1px solid rgba(255, 255, 255, 0.18);
  }

  .construction-principles article {
    display: grid;
    grid-template-columns: 3rem 1fr;
    gap: 1rem;
    border-bottom: 1px solid rgba(255, 255, 255, 0.18);
    padding-block: 1.2rem;
  }

  .construction-principles article > span {
    color: rgba(246, 243, 235, 0.4);
    font-size: 0.58rem;
    font-weight: 800;
  }

  .construction-principles h3 {
    margin: 0;
    color: #ffffff;
    font-size: 0.82rem;
  }

  .construction-principles article p {
    max-width: 42rem;
    margin: 0.45rem 0 0;
    color: rgba(246, 243, 235, 0.65);
    font-size: 0.7rem;
    line-height: 1.65;
  }

  @media (max-width: 52rem) {
    .construction-principles__layout {
      grid-template-columns: 1fr;
    }
  }
</style>
