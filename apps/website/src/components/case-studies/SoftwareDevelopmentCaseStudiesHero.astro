<section
  class="software-case-hero"
  aria-labelledby="software-case-hero-title"
>
  <div class="site-container software-case-hero__layout">
    <div>
      <p class="software-case-hero__eyebrow">
        Case Studies / Software Development
      </p>

      <h1 id="software-case-hero-title">
        Software projects should be judged by
        <span>what they improve operationally.</span>
      </h1>
    </div>

    <div class="software-case-hero__context">
      <p>
        Selected software development work from LKProfessionals,
        documenting the business problem, system architecture,
        implementation decisions and operational results.
      </p>

      <dl>
        <div>
          <dt>Systems</dt>
          <dd>ERP · CRM · POS · SaaS · Custom Software</dd>
        </div>

        <div>
          <dt>Focus</dt>
          <dd>Workflow · Data · Integration · Operations</dd>
        </div>
      </dl>
    </div>
  </div>

  <div
    class="site-container software-case-hero__rail"
    aria-hidden="true"
  >
    <span>Problem</span>
    <i></i>
    <span>Workflow</span>
    <i></i>
    <span>Architecture</span>
    <i></i>
    <span>Implementation</span>
    <i></i>
    <span>Outcome</span>
  </div>
</section>

<style>
  .software-case-hero {
    position: relative;
    overflow: hidden;
    border-bottom: 1px solid #dde4ed;
    background: #f7f9fc;
    padding-block:
      clamp(5rem, 10vw, 9rem)
      clamp(3rem, 6vw, 4.5rem);
  }

  .software-case-hero::before {
    position: absolute;
    inset: 0;
    background:
      linear-gradient(
        90deg,
        rgba(11, 47, 143, 0.035) 1px,
        transparent 1px
      ),
      linear-gradient(
        rgba(11, 47, 143, 0.035) 1px,
        transparent 1px
      );
    background-size: 4.5rem 4.5rem;
    content: "";
    pointer-events: none;
  }

  .software-case-hero__layout,
  .software-case-hero__rail {
    position: relative;
    z-index: 1;
  }

  .software-case-hero__layout {
    display: grid;
    grid-template-columns:
      minmax(0, 1fr)
      minmax(17rem, 0.38fr);
    align-items: end;
    gap: clamp(3rem, 9vw, 8rem);
  }

  .software-case-hero__eyebrow {
    margin: 0 0 1rem;
    color: #0b2f8f;
    font-size: 0.65rem;
    font-weight: 800;
    letter-spacing: 0.15em;
    text-transform: uppercase;
  }

  .software-case-hero h1 {
    max-width: 12ch;
    margin: 0;
    color: #0b1e3a;
    font-family: var(--font-serif);
    font-size: clamp(3.8rem, 8vw, 7.3rem);
    font-weight: 400;
    letter-spacing: -0.06em;
    line-height: 0.92;
  }

  .software-case-hero h1 span {
    display: block;
    color: #0b2f8f;
  }

  .software-case-hero__context {
    border-left: 1px solid #cdd6e2;
    padding-left: 1.4rem;
  }

  .software-case-hero__context > p {
    margin: 0;
    color: #6e7b8d;
    font-size: 0.78rem;
    line-height: 1.75;
  }

  .software-case-hero dl {
    display: grid;
    gap: 0.8rem;
    border-top: 1px solid #d8e0e9;
    margin: 1.5rem 0 0;
    padding-top: 1rem;
  }

  .software-case-hero dl div {
    display: grid;
    grid-template-columns: 5rem 1fr;
    gap: 1rem;
  }

  .software-case-hero dt {
    color: #8a95a5;
    font-size: 0.55rem;
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: 0.08em;
  }

  .software-case-hero dd {
    margin: 0;
    color: #293b53;
    font-size: 0.66rem;
    font-weight: 700;
  }

  .software-case-hero__rail {
    display: grid;
    grid-template-columns:
      auto 1fr auto 1fr auto 1fr auto 1fr auto;
    align-items: center;
    gap: 0.75rem;
    border-top: 1px solid #d9e0e9;
    margin-top: clamp(4rem, 8vw, 7rem);
    padding-top: 1.25rem;
  }

  .software-case-hero__rail span {
    color: #53647a;
    font-size: 0.58rem;
    font-weight: 750;
  }

  .software-case-hero__rail i {
    height: 1px;
    background: #cfd8e4;
  }

  @media (max-width: 52rem) {
    .software-case-hero__layout {
      grid-template-columns: 1fr;
      align-items: start;
    }
  }

  @media (max-width: 38rem) {
    .software-case-hero h1 {
      font-size: clamp(3rem, 15vw, 5rem);
    }

    .software-case-hero__rail {
      display: none;
    }
  }
</style>
