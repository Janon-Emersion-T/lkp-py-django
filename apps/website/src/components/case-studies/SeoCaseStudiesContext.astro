<section class="seo-case-context">
  <div class="site-container seo-case-context__layout">
    <div>
      <p>What we document</p>

      <h2>
        SEO results need context before they need celebration.
      </h2>
    </div>

    <div class="seo-case-context__list">
      <article>
        <span>01</span>

        <div>
          <h3>The search baseline</h3>
          <p>
            Existing visibility, indexation, technical issues,
            content coverage and the competitive search environment.
          </p>
        </div>
      </article>

      <article>
        <span>02</span>

        <div>
          <h3>The search opportunity</h3>
          <p>
            Which queries, pages and customer intents were worth
            prioritising instead of chasing meaningless traffic.
          </p>
        </div>
      </article>

      <article>
        <span>03</span>

        <div>
          <h3>The optimisation work</h3>
          <p>
            Technical fixes, information architecture, content,
            on-page signals, local signals and other implemented work.
          </p>
        </div>
      </article>

      <article>
        <span>04</span>

        <div>
          <h3>The measurable change</h3>
          <p>
            Visibility, qualified organic traffic, enquiries and
            other outcomes that can be evaluated against the baseline.
          </p>
        </div>
      </article>
    </div>
  </div>
</section>

<style>
  .seo-case-context {
    padding-block: clamp(5rem, 10vw, 9rem);
    background: #0b2f8f;
    color: #f6f3eb;
  }

  .seo-case-context__layout {
    display: grid;
    grid-template-columns:
      minmax(17rem, 0.72fr)
      minmax(0, 1fr);
    gap: clamp(3rem, 8vw, 7rem);
  }

  .seo-case-context__layout
    > div:first-child > p {
    margin: 0 0 1rem;
    color: rgba(246, 243, 235, 0.56);
    font-size: 0.64rem;
    font-weight: 800;
    letter-spacing: 0.14em;
    text-transform: uppercase;
  }

  .seo-case-context h2 {
    max-width: 12ch;
    margin: 0;
    font-family: var(--font-serif);
    font-size: clamp(2.6rem, 5vw, 4.7rem);
    font-weight: 400;
    letter-spacing: -0.05em;
    line-height: 1;
  }

  .seo-case-context__list {
    border-top: 1px solid rgba(255, 255, 255, 0.18);
  }

  .seo-case-context article {
    display: grid;
    grid-template-columns: 3rem 1fr;
    gap: 1rem;
    border-bottom: 1px solid rgba(255, 255, 255, 0.18);
    padding-block: 1.2rem;
  }

  .seo-case-context article > span {
    color: rgba(246, 243, 235, 0.4);
    font-size: 0.58rem;
    font-weight: 800;
  }

  .seo-case-context h3 {
    margin: 0;
    color: #ffffff;
    font-size: 0.82rem;
  }

  .seo-case-context article p {
    max-width: 42rem;
    margin: 0.45rem 0 0;
    color: rgba(246, 243, 235, 0.65);
    font-size: 0.7rem;
    line-height: 1.65;
  }

  @media (max-width: 52rem) {
    .seo-case-context__layout {
      grid-template-columns: 1fr;
    }
  }
</style>
