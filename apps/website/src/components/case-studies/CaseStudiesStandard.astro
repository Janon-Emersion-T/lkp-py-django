<section class="case-standard">
  <div class="site-container case-standard__layout">
    <div>
      <p>Our case-study standard</p>

      <h2>
        A portfolio should tell you what happened, not only what it looked like.
      </h2>
    </div>

    <div class="case-standard__list">
      <article>
        <span>01</span>

        <div>
          <h3>Describe the starting point</h3>
          <p>
            Results are meaningless without understanding the
            problem, constraints and condition before the work began.
          </p>
        </div>
      </article>

      <article>
        <span>02</span>

        <div>
          <h3>Explain the decisions</h3>
          <p>
            A case study should show why important product,
            technical or marketing decisions were made.
          </p>
        </div>
      </article>

      <article>
        <span>03</span>

        <div>
          <h3>Separate work from claims</h3>
          <p>
            We distinguish what was actually delivered from
            outcomes that can reasonably be attributed to it.
          </p>
        </div>
      </article>

      <article>
        <span>04</span>

        <div>
          <h3>Use evidence where available</h3>
          <p>
            Metrics are useful when they have context. We avoid
            manufacturing numbers simply to make a project sound bigger.
          </p>
        </div>
      </article>
    </div>
  </div>
</section>

<style>
  .case-standard {
    padding-block: clamp(5rem, 10vw, 9rem);
    background: #0b2f8f;
    color: #f6f3eb;
  }

  .case-standard__layout {
    display: grid;
    grid-template-columns:
      minmax(17rem, 0.72fr)
      minmax(0, 1fr);
    gap: clamp(3rem, 8vw, 7rem);
  }

  .case-standard__layout
    > div:first-child > p {
    margin: 0 0 1rem;
    color: rgba(246, 243, 235, 0.56);
    font-size: 0.64rem;
    font-weight: 800;
    letter-spacing: 0.14em;
    text-transform: uppercase;
  }

  .case-standard h2 {
    max-width: 12ch;
    margin: 0;
    font-family: var(--font-serif);
    font-size: clamp(2.6rem, 5vw, 4.7rem);
    font-weight: 400;
    letter-spacing: -0.05em;
    line-height: 1;
  }

  .case-standard__list {
    border-top: 1px solid rgba(255, 255, 255, 0.18);
  }

  .case-standard article {
    display: grid;
    grid-template-columns:
      3rem
      1fr;
    gap: 1rem;
    border-bottom: 1px solid rgba(255, 255, 255, 0.18);
    padding-block: 1.2rem;
  }

  .case-standard article > span {
    color: rgba(246, 243, 235, 0.4);
    font-size: 0.58rem;
    font-weight: 800;
  }

  .case-standard h3 {
    margin: 0;
    color: #ffffff;
    font-size: 0.82rem;
  }

  .case-standard article p {
    max-width: 42rem;
    margin: 0.45rem 0 0;
    color: rgba(246, 243, 235, 0.65);
    font-size: 0.7rem;
    line-height: 1.65;
  }

  @media (max-width: 52rem) {
    .case-standard__layout {
      grid-template-columns: 1fr;
    }
  }
</style>
