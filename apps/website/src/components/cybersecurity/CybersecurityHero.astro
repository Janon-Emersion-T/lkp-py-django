<section
  class="security-hero"
  aria-labelledby="security-hero-title"
>
  <div class="site-container security-hero__inner">
    <div class="security-hero__content">
      <p class="security-hero__eyebrow">
        Cybersecurity
      </p>

      <h1 id="security-hero-title">
        Reduce risk before
        <span>it becomes disruption.</span>
      </h1>

      <p class="security-hero__lead">
        LKProfessionals helps businesses strengthen websites,
        applications and technical environments through security
        review, hardening, backup planning and practical risk
        reduction.
      </p>
    </div>

    <aside class="security-hero__aside">
      <span>Security objective</span>

      <p>
        Reduce exposure.<br />
        Improve resilience.<br />
        Prepare recovery.
      </p>
    </aside>
  </div>

  <div
    class="site-container security-hero__flow"
    aria-hidden="true"
  >
    <span>Identify</span>
    <i></i>
    <span>Reduce</span>
    <i></i>
    <span>Protect</span>
    <i></i>
    <span>Monitor</span>
    <i></i>
    <span>Recover</span>
  </div>
</section>

<style>
  .security-hero {
    position: relative;
    overflow: hidden;
    border-bottom: 1px solid #dde4ed;
    background: #f7f9fc;
    padding-block:
      clamp(4.5rem, 9vw, 8rem)
      clamp(2.8rem, 5vw, 4rem);
  }

  .security-hero::before {
    position: absolute;
    inset: 0;
    background:
      linear-gradient(
        90deg,
        rgba(11, 47, 143, 0.035) 1px,
        transparent 1px
      ),
      linear-gradient(
        rgba(11, 47, 143, 0.035) 1px,
        transparent 1px
      );
    background-size: 4.5rem 4.5rem;
    content: "";
    pointer-events: none;
  }

  .security-hero__inner,
  .security-hero__flow {
    position: relative;
    z-index: 1;
  }

  .security-hero__inner {
    display: grid;
    grid-template-columns:
      minmax(0, 1fr)
      minmax(14rem, 0.3fr);
    align-items: end;
    gap: clamp(3rem, 9vw, 8rem);
  }

  .security-hero__eyebrow {
    margin: 0 0 1rem;
    color: #0b2f8f;
    font-size: 0.67rem;
    font-weight: 800;
    letter-spacing: 0.15em;
    text-transform: uppercase;
  }

  .security-hero h1 {
    max-width: 11ch;
    margin: 0;
    color: #0b1e3a;
    font-family: var(--font-serif);
    font-size: clamp(3.7rem, 8vw, 7.3rem);
    font-weight: 400;
    letter-spacing: -0.06em;
    line-height: 0.92;
  }

  .security-hero h1 span {
    display: block;
    color: #0b2f8f;
  }

  .security-hero__lead {
    max-width: 45rem;
    margin: 1.6rem 0 0;
    color: #687589;
    font-size: clamp(0.9rem, 1.2vw, 1rem);
    line-height: 1.8;
  }

  .security-hero__aside {
    border-left: 1px solid #cad4e1;
    padding-left: 1.25rem;
  }

  .security-hero__aside span {
    color: #8792a2;
    font-size: 0.59rem;
    font-weight: 800;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  .security-hero__aside p {
    margin: 0.55rem 0 0;
    color: #293b53;
    font-size: 0.76rem;
    font-weight: 700;
    line-height: 1.75;
  }

  .security-hero__flow {
    display: grid;
    grid-template-columns:
      auto 1fr auto 1fr auto 1fr auto 1fr auto;
    align-items: center;
    gap: 0.75rem;
    border-top: 1px solid #d9e0e9;
    margin-top: clamp(3.5rem, 7vw, 6rem);
    padding-top: 1.25rem;
  }

  .security-hero__flow span {
    color: #53647a;
    font-size: 0.58rem;
    font-weight: 750;
  }

  .security-hero__flow i {
    height: 1px;
    background: #cfd8e4;
  }

  @media (max-width: 52rem) {
    .security-hero__inner {
      grid-template-columns: 1fr;
      align-items: start;
    }
  }

  @media (max-width: 38rem) {
    .security-hero h1 {
      font-size: clamp(3.1rem, 16vw, 5rem);
    }

    .security-hero__flow {
      display: none;
    }
  }
</style>
