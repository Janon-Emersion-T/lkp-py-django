<section class="da-overview" aria-labelledby="da-overview-title">
  <div class="site-container da-overview__layout">
    <div>
      <p>Why analytics matters</p>

      <h2 id="da-overview-title">
        Businesses often have more data than visibility.
      </h2>
    </div>

    <div class="da-overview__copy">
      <p>
        Sales systems, accounting platforms, CRMs, websites,
        operational software and spreadsheets all generate
        information. The problem is that the information is often
        fragmented or difficult to interpret.
      </p>

      <p>
        Effective analytics brings those signals into a structure
        that helps management understand performance, identify
        exceptions and recognise trends earlier.
      </p>

      <p>
        The objective is not more reporting. It is clearer
        operational understanding.
      </p>
    </div>
  </div>
</section>

<style>
  .da-overview {
    padding-block: clamp(5rem, 10vw, 9rem);
    background: #ffffff;
  }

  .da-overview__layout {
    display: grid;
    grid-template-columns:
      minmax(16rem, .7fr)
      minmax(0, 1fr);
    gap: clamp(3rem, 9vw, 8rem);
  }

  .da-overview__layout
    > div:first-child > p {
    margin: 0 0 1rem;
    color: #0b2f8f;
    font-size: .64rem;
    font-weight: 800;
    letter-spacing: .14em;
    text-transform: uppercase;
  }

  .da-overview h2 {
    max-width: 11ch;
    margin: 0;
    color: #0d203c;
    font-family: var(--font-serif);
    font-size: clamp(2.6rem, 5vw, 4.8rem);
    font-weight: 400;
    letter-spacing: -.05em;
    line-height: 1;
  }

  .da-overview__copy {
    display: grid;
    gap: 1.15rem;
  }

  .da-overview__copy p {
    max-width: 44rem;
    margin: 0;
    color: #6f7c90;
    font-size: .84rem;
    line-height: 1.8;
  }

  @media (max-width: 52rem) {
    .da-overview__layout {
      grid-template-columns: 1fr;
    }
  }
</style>
