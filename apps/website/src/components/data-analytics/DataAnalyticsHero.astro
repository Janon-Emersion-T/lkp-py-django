<section class="da-hero" aria-labelledby="da-hero-title">
  <div class="site-container da-hero__inner">
    <div class="da-hero__content">
      <p class="da-hero__eyebrow">
        Data & Analytics
      </p>

      <h1 id="da-hero-title">
        Turn business data into
        <span>useful direction.</span>
      </h1>

      <p class="da-hero__lead">
        We design dashboards, reporting systems, business
        intelligence solutions and KPI tracking that help
        organisations understand what is happening and make
        decisions with better information.
      </p>
    </div>

    <aside class="da-hero__aside">
      <span>From data to action</span>

      <p>
        Collect.<br />
        Understand.<br />
        Decide.
      </p>
    </aside>
  </div>

  <div class="site-container da-hero__flow" aria-hidden="true">
    <span>Sources</span>
    <i></i>
    <span>Structure</span>
    <i></i>
    <span>Analysis</span>
    <i></i>
    <span>Insight</span>
    <i></i>
    <span>Decision</span>
  </div>
</section>

<style>
  .da-hero {
    position: relative;
    overflow: hidden;
    border-bottom: 1px solid #dde4ed;
    background: #f7f9fc;
    padding-block:
      clamp(4.5rem, 9vw, 8rem)
      clamp(2.8rem, 5vw, 4rem);
  }

  .da-hero::before {
    position: absolute;
    inset: 0;
    background:
      linear-gradient(
        90deg,
        rgba(11,47,143,.035) 1px,
        transparent 1px
      ),
      linear-gradient(
        rgba(11,47,143,.035) 1px,
        transparent 1px
      );
    background-size: 4.5rem 4.5rem;
    content: "";
    pointer-events: none;
  }

  .da-hero__inner,
  .da-hero__flow {
    position: relative;
    z-index: 1;
  }

  .da-hero__inner {
    display: grid;
    grid-template-columns:
      minmax(0, 1fr)
      minmax(14rem, .3fr);
    align-items: end;
    gap: clamp(3rem, 9vw, 8rem);
  }

  .da-hero__eyebrow {
    margin: 0 0 1rem;
    color: #0b2f8f;
    font-size: .67rem;
    font-weight: 800;
    letter-spacing: .15em;
    text-transform: uppercase;
  }

  .da-hero h1 {
    max-width: 11ch;
    margin: 0;
    color: #0b1e3a;
    font-family: var(--font-serif);
    font-size: clamp(3.7rem, 8vw, 7.3rem);
    font-weight: 400;
    letter-spacing: -.06em;
    line-height: .92;
  }

  .da-hero h1 span {
    display: block;
    color: #0b2f8f;
  }

  .da-hero__lead {
    max-width: 45rem;
    margin: 1.6rem 0 0;
    color: #687589;
    font-size: clamp(.9rem, 1.2vw, 1rem);
    line-height: 1.8;
  }

  .da-hero__aside {
    border-left: 1px solid #cad4e1;
    padding-left: 1.25rem;
  }

  .da-hero__aside span {
    color: #8792a2;
    font-size: .59rem;
    font-weight: 800;
    letter-spacing: .12em;
    text-transform: uppercase;
  }

  .da-hero__aside p {
    margin: .55rem 0 0;
    color: #293b53;
    font-size: .76rem;
    font-weight: 700;
    line-height: 1.75;
  }

  .da-hero__flow {
    display: grid;
    grid-template-columns:
      auto 1fr auto 1fr auto 1fr auto 1fr auto;
    align-items: center;
    gap: .75rem;
    border-top: 1px solid #d9e0e9;
    margin-top: clamp(3.5rem, 7vw, 6rem);
    padding-top: 1.25rem;
  }

  .da-hero__flow span {
    color: #53647a;
    font-size: .58rem;
    font-weight: 750;
  }

  .da-hero__flow i {
    height: 1px;
    background: #cfd8e4;
  }

  @media (max-width: 52rem) {
    .da-hero__inner {
      grid-template-columns: 1fr;
      align-items: start;
    }
  }

  @media (max-width: 38rem) {
    .da-hero h1 {
      font-size: clamp(3.1rem, 16vw, 5rem);
    }

    .da-hero__flow {
      display: none;
    }
  }
</style>
