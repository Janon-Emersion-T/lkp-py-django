<section
  class="footer-newsletter-shell"
  aria-labelledby="footer-newsletter-title"
>
  <div class="site-container">
    <div class="footer-newsletter">
      <div class="footer-newsletter__icon" aria-hidden="true">
        <svg width="27" height="27" viewBox="0 0 27 27">
          <rect
            x="3.5"
            y="5.5"
            width="20"
            height="16"
            rx="1.5"
            fill="none"
            stroke="currentColor"
            stroke-width="1.7"
          />
          <path
            d="m4.5 7 9 7 9-7"
            fill="none"
            stroke="currentColor"
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="1.7"
          />
        </svg>
      </div>

      <div class="footer-newsletter__heading">
        <p>Newsletter</p>

        <h2 id="footer-newsletter-title">
          Stay updated with insights that move your business forward.
        </h2>
      </div>

      <p class="footer-newsletter__description">
        Practical updates on technology, digital growth and business
        systems—delivered occasionally.
      </p>

      <form
        class="footer-newsletter__form"
        action="/newsletter/subscribe"
        method="post"
      >
        <label class="sr-only" for="footer-newsletter-email">
          Work email address
        </label>

        <div class="footer-newsletter__field">
          <div class="footer-newsletter__input">
            <svg aria-hidden="true" width="18" height="18" viewBox="0 0 24 24">
              <rect
                x="3"
                y="5"
                width="18"
                height="14"
                rx="2"
                fill="none"
                stroke="currentColor"
                stroke-width="1.6"
              />
              <path
                d="m4 7 8 6 8-6"
                fill="none"
                stroke="currentColor"
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="1.6"
              />
            </svg>

            <input
              id="footer-newsletter-email"
              name="email"
              type="email"
              autocomplete="email"
              inputmode="email"
              placeholder="Your work email"
              required
            />
          </div>

          <button type="submit">
            <span>Subscribe</span>

            <svg aria-hidden="true" width="17" height="17" viewBox="0 0 17 17">
              <path
                d="M3 8.5h11M10 4.5l4 4-4 4"
                fill="none"
                stroke="currentColor"
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="1.5"
              />
            </svg>
          </button>
        </div>

        <p class="footer-newsletter__privacy">
          By subscribing, you agree to our
          <a href="/legal/privacy-policy">Privacy Policy</a>.
        </p>
      </form>
    </div>
  </div>
</section>

<style>
  .footer-newsletter-shell {
    position: relative;
    z-index: 2;
    background: var(--colour-white);
    padding-block: clamp(2.5rem, 5vw, 4.5rem);
  }

  .footer-newsletter {
    display: grid;
    grid-template-columns:
      auto
      minmax(16rem, 1.15fr)
      minmax(13rem, 0.8fr)
      minmax(21rem, 1.25fr);
    align-items: center;
    gap: clamp(1.5rem, 3.5vw, 3.5rem);
    border: 1px solid #dfe6f0;
    border-radius: 1.25rem;
    background:
      linear-gradient(135deg, rgba(11, 47, 143, 0.025), transparent 55%),
      #f8fafc;
    padding: clamp(1.5rem, 3.5vw, 2.75rem);
    box-shadow: 0 18px 50px rgba(7, 26, 61, 0.07);
  }

  .footer-newsletter__icon {
    display: inline-flex;
    width: 4.3rem;
    height: 4.3rem;
    align-items: center;
    justify-content: center;
    border: 1px solid rgba(11, 47, 143, 0.12);
    border-radius: 50%;
    background: #eaf1ff;
    color: #0b2f8f;
  }

  .footer-newsletter__heading {
    min-width: 0;
  }

  .footer-newsletter__heading > p {
    margin: 0 0 0.45rem;
    color: #0b2f8f;
    font-size: 0.67rem;
    font-weight: 780;
    letter-spacing: 0.16em;
    text-transform: uppercase;
  }

  .footer-newsletter h2 {
    margin: 0;
    color: #071a3d;
    font-family: var(--font-sans);
    font-size: clamp(1.2rem, 2.1vw, 1.65rem);
    font-weight: 760;
    letter-spacing: -0.025em;
    line-height: 1.3;
  }

  .footer-newsletter__description {
    margin: 0;
    color: #5b6472;
    font-size: 0.88rem;
    line-height: 1.7;
  }

  .footer-newsletter__form {
    min-width: 0;
  }

  .footer-newsletter__field {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    gap: 0.65rem;
  }

  .footer-newsletter__input {
    display: flex;
    min-height: 3.45rem;
    align-items: center;
    gap: 0.75rem;
    border: 1px solid #cad4e2;
    border-radius: 0.65rem;
    background: #ffffff;
    color: #667085;
    padding-inline: 1rem;
    transition:
      border-color 170ms ease,
      box-shadow 170ms ease;
  }

  .footer-newsletter__input:focus-within {
    border-color: #1746c7;
    box-shadow: 0 0 0 4px rgba(23, 70, 199, 0.09);
  }

  .footer-newsletter input {
    width: 100%;
    min-width: 0;
    border: 0;
    outline: 0;
    background: transparent;
    color: #111827;
    padding: 0;
  }

  .footer-newsletter input::placeholder {
    color: #8b94a7;
  }

  .footer-newsletter button {
    display: inline-flex;
    min-height: 3.45rem;
    align-items: center;
    justify-content: center;
    gap: 0.65rem;
    border: 0;
    border-radius: 0.65rem;
    background: #0b2f8f;
    color: #ffffff;
    cursor: pointer;
    padding-inline: 1.35rem;
    font-size: 0.83rem;
    font-weight: 760;
    transition:
      background-color 170ms ease,
      transform 170ms ease;
  }

  .footer-newsletter button:hover {
    transform: translateY(-2px);
    background: #1746c7;
  }

  .footer-newsletter button svg {
    transition: transform 170ms ease;
  }

  .footer-newsletter button:hover svg {
    transform: translateX(0.2rem);
  }

  .footer-newsletter__privacy {
    margin: 0.65rem 0 0;
    color: #7a8494;
    font-size: 0.67rem;
    line-height: 1.45;
  }

  .footer-newsletter__privacy a {
    color: #0b2f8f;
    text-decoration: underline;
    text-underline-offset: 0.16rem;
  }

  .sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    overflow: hidden;
    margin: -1px;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
  }

  @media (max-width: 68rem) {
    .footer-newsletter {
      grid-template-columns: auto minmax(0, 1fr) minmax(20rem, 1fr);
    }

    .footer-newsletter__description {
      display: none;
    }
  }

  @media (max-width: 50rem) {
    .footer-newsletter {
      grid-template-columns: auto minmax(0, 1fr);
    }

    .footer-newsletter__form {
      grid-column: 1 / -1;
    }
  }

  @media (max-width: 36rem) {
    .footer-newsletter {
      grid-template-columns: 1fr;
      justify-items: center;
      text-align: center;
      border-radius: 1rem;
    }

    .footer-newsletter__form {
      width: 100%;
      grid-column: auto;
    }

    .footer-newsletter__field {
      grid-template-columns: 1fr;
    }

    .footer-newsletter button {
      width: 100%;
    }
  }
</style>
