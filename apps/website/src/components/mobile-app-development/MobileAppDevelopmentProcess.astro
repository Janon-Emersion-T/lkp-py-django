---
import {
  mobileAppStages,
} from "../../config/mobile-app-development";
---

<section
  class="mobile-process"
  aria-labelledby="mobile-process-title"
>
  <div class="site-container">
    <header class="mobile-process__heading">
      <p>From idea to release</p>

      <h2 id="mobile-process-title">
        Build the product around the real mobile journey.
      </h2>
    </header>

    <div class="mobile-process__steps">
      {
        mobileAppStages.map((stage) => (
          <article>
            <span>{stage.number}</span>
            <h3>{stage.title}</h3>
            <p>{stage.description}</p>
          </article>
        ))
      }
    </div>
  </div>
</section>

<style>
  .mobile-process {
    padding-block: clamp(4.5rem, 9vw, 8rem);
    background: #ffffff;
  }

  .mobile-process__heading {
    max-width: 48rem;
    margin-bottom: 3rem;
  }

  .mobile-process__heading > p {
    margin: 0 0 1rem;
    color: #0b2f8f;
    font-size: 0.64rem;
    font-weight: 800;
    letter-spacing: 0.14em;
    text-transform: uppercase;
  }

  .mobile-process h2 {
    max-width: 13ch;
    margin: 0;
    color: #0d203c;
    font-family: var(--font-serif);
    font-size: clamp(2.5rem, 5vw, 4.6rem);
    font-weight: 400;
    letter-spacing: -0.05em;
    line-height: 1;
  }

  .mobile-process__steps {
    display: grid;
    grid-template-columns:
      repeat(6, minmax(0, 1fr));
    border-top: 1px solid #dce3ec;
  }

  .mobile-process article {
    border-right: 1px solid #dce3ec;
    border-bottom: 1px solid #dce3ec;
    padding: 1.3rem 1rem;
  }

  .mobile-process article:first-child {
    padding-left: 0;
  }

  .mobile-process article:last-child {
    border-right: 0;
  }

  .mobile-process article > span {
    color: #0b2f8f;
    font-size: 0.58rem;
    font-weight: 800;
  }

  .mobile-process h3 {
    margin: 1rem 0 0;
    color: #283a52;
    font-size: 0.8rem;
  }

  .mobile-process article p {
    margin: 0.55rem 0 0;
    color: #788497;
    font-size: 0.67rem;
    line-height: 1.65;
  }

  @media (max-width: 64rem) {
    .mobile-process__steps {
      grid-template-columns:
        repeat(3, minmax(0, 1fr));
    }
  }

  @media (max-width: 42rem) {
    .mobile-process__steps {
      grid-template-columns:
        repeat(2, minmax(0, 1fr));
    }
  }

  @media (max-width: 30rem) {
    .mobile-process__steps {
      grid-template-columns: 1fr;
    }

    .mobile-process article,
    .mobile-process article:first-child {
      border-right: 0;
      padding-inline: 0;
    }
  }
</style>
