<section
  class="mobile-overview"
  aria-labelledby="mobile-overview-title"
>
  <div class="site-container mobile-overview__layout">
    <div>
      <p>Why mobile</p>

      <h2 id="mobile-overview-title">
        A mobile app should solve a mobility problem.
      </h2>
    </div>

    <div class="mobile-overview__copy">
      <p>
        Not every business needs a mobile application. A responsive
        website may already provide the right experience for many
        customer journeys.
      </p>

      <p>
        Mobile applications become valuable when users need repeated
        access, device capabilities, persistent authentication,
        notifications, offline behaviour or workflows that happen
        away from a desk.
      </p>

      <p>
        We start by understanding that requirement before deciding
        whether Android, iOS, cross-platform development or another
        delivery approach makes the most sense.
      </p>
    </div>
  </div>
</section>

<style>
  .mobile-overview {
    padding-block: clamp(5rem, 10vw, 9rem);
    background: #ffffff;
  }

  .mobile-overview__layout {
    display: grid;
    grid-template-columns:
      minmax(16rem, 0.7fr)
      minmax(0, 1fr);
    gap: clamp(3rem, 9vw, 8rem);
  }

  .mobile-overview__layout
    > div:first-child > p {
    margin: 0 0 1rem;
    color: #0b2f8f;
    font-size: 0.64rem;
    font-weight: 800;
    letter-spacing: 0.14em;
    text-transform: uppercase;
  }

  .mobile-overview h2 {
    max-width: 11ch;
    margin: 0;
    color: #0d203c;
    font-family: var(--font-serif);
    font-size: clamp(2.6rem, 5vw, 4.8rem);
    font-weight: 400;
    letter-spacing: -0.05em;
    line-height: 1;
  }

  .mobile-overview__copy {
    display: grid;
    gap: 1.15rem;
  }

  .mobile-overview__copy p {
    max-width: 44rem;
    margin: 0;
    color: #6f7c90;
    font-size: 0.84rem;
    line-height: 1.8;
  }

  @media (max-width: 52rem) {
    .mobile-overview__layout {
      grid-template-columns: 1fr;
    }
  }
</style>
