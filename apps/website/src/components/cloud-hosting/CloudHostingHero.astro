<section
  class="cloud-hero"
  aria-labelledby="cloud-hero-title"
>
  <div class="site-container cloud-hero__inner">
    <div class="cloud-hero__content">
      <p class="cloud-hero__eyebrow">
        Cloud & Hosting
      </p>

      <h1 id="cloud-hero-title">
        Infrastructure should stay
        <span>out of your way.</span>
      </h1>

      <p class="cloud-hero__lead">
        LKProfessionals provides hosting, server, domain,
        business email and SSL services designed to keep the
        infrastructure behind your digital operations dependable
        and manageable.
      </p>
    </div>

    <aside class="cloud-hero__aside">
      <span>Infrastructure objective</span>

      <p>
        Stay available.<br />
        Stay controlled.<br />
        Stay maintainable.
      </p>
    </aside>
  </div>

  <div
    class="site-container cloud-hero__flow"
    aria-hidden="true"
  >
    <span>Domain</span>
    <i></i>
    <span>DNS</span>
    <i></i>
    <span>Server</span>
    <i></i>
    <span>Application</span>
    <i></i>
    <span>User</span>
  </div>
</section>

<style>
  .cloud-hero {
    position: relative;
    overflow: hidden;
    border-bottom: 1px solid #dde4ed;
    background: #f7f9fc;
    padding-block:
      clamp(4.5rem, 9vw, 8rem)
      clamp(2.8rem, 5vw, 4rem);
  }

  .cloud-hero::before {
    position: absolute;
    inset: 0;
    background:
      linear-gradient(
        90deg,
        rgba(11, 47, 143, 0.035) 1px,
        transparent 1px
      ),
      linear-gradient(
        rgba(11, 47, 143, 0.035) 1px,
        transparent 1px
      );
    background-size: 4.5rem 4.5rem;
    content: "";
    pointer-events: none;
  }

  .cloud-hero__inner,
  .cloud-hero__flow {
    position: relative;
    z-index: 1;
  }

  .cloud-hero__inner {
    display: grid;
    grid-template-columns:
      minmax(0, 1fr)
      minmax(14rem, 0.3fr);
    align-items: end;
    gap: clamp(3rem, 9vw, 8rem);
  }

  .cloud-hero__eyebrow {
    margin: 0 0 1rem;
    color: #0b2f8f;
    font-size: 0.67rem;
    font-weight: 800;
    letter-spacing: 0.15em;
    text-transform: uppercase;
  }

  .cloud-hero h1 {
    max-width: 11ch;
    margin: 0;
    color: #0b1e3a;
    font-family: var(--font-serif);
    font-size: clamp(3.7rem, 8vw, 7.3rem);
    font-weight: 400;
    letter-spacing: -0.06em;
    line-height: 0.92;
  }

  .cloud-hero h1 span {
    display: block;
    color: #0b2f8f;
  }

  .cloud-hero__lead {
    max-width: 45rem;
    margin: 1.6rem 0 0;
    color: #687589;
    font-size: clamp(0.9rem, 1.2vw, 1rem);
    line-height: 1.8;
  }

  .cloud-hero__aside {
    border-left: 1px solid #cad4e1;
    padding-left: 1.25rem;
  }

  .cloud-hero__aside span {
    color: #8792a2;
    font-size: 0.59rem;
    font-weight: 800;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  .cloud-hero__aside p {
    margin: 0.55rem 0 0;
    color: #293b53;
    font-size: 0.76rem;
    font-weight: 700;
    line-height: 1.75;
  }

  .cloud-hero__flow {
    display: grid;
    grid-template-columns:
      auto 1fr auto 1fr auto 1fr auto 1fr auto;
    align-items: center;
    gap: 0.75rem;
    border-top: 1px solid #d9e0e9;
    margin-top: clamp(3.5rem, 7vw, 6rem);
    padding-top: 1.25rem;
  }

  .cloud-hero__flow span {
    color: #53647a;
    font-size: 0.58rem;
    font-weight: 750;
  }

  .cloud-hero__flow i {
    height: 1px;
    background: #cfd8e4;
  }

  @media (max-width: 52rem) {
    .cloud-hero__inner {
      grid-template-columns: 1fr;
      align-items: start;
    }
  }

  @media (max-width: 38rem) {
    .cloud-hero h1 {
      font-size: clamp(3.1rem, 16vw, 5rem);
    }

    .cloud-hero__flow {
      display: none;
    }
  }
</style>
