{
  "name": "lkprofessionals-new",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/website",
    "apps/dashboard"
  ],
  "scripts": {
    "dev:website": "npm run dev --workspace=website",
    "dev:dashboard": "npm run dev --workspace=dashboard",
    "build": "npm run build --workspace=website && npm run build --workspace=dashboard",
    "check": "npm run check --workspace=website && npm run check --workspace=dashboard"
  }
}
